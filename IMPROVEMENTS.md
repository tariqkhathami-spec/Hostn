# Hostn — Fixes & Improvements Tracker

> **Website:** hostn.co
> **Repo (upstream):** https://github.com/tariqkhathami-spec/Hostn
> **Repo (fork):** https://github.com/abdullahsalma/hostn

---

## Instructions
- This file tracks all fixes and improvements made to the Hostn project.
- Completed items stay in the lists for reference — never remove them.
- Each entry includes a date, short description, author (who suggested it), and status.
- **Each bullet point from the user is a separate task. Never merge, combine, or skip tasks. Each one gets its own entry and must be addressed individually.**

---

## Users

| Initials | Name |
|----------|------|
| AMS | Abdullah |
| TAK | Tariq |

---

## Fixes

| # | Date | Description | Author | Remark | Status |
|---|------|-------------|--------|--------|--------|
| F1 | 2026-04-03 | `/auth/guest/login` — Cursor between +966 prefix and phone field | AMS | Added autoFocus on phone input | Done |
| F2 | 2026-04-03 | `/auth/guest/login` — Enter key does nothing after typing phone | AMS | Added onKeyDown Enter handler to send OTP | Done |
| F3 | 2026-04-03 | `/host/listings/new` — Editing listing does not show original city and price | AMS | Fixed data mapping: `p.location.city`, `p.pricing.perNight`, `p.capacity.*` | Done |
| F4 | 2026-04-03 | `/` — Clicking city opens calendar automatically; calendar scrolls with page | AMS | Removed auto-open calendar after city select; calendar uses fixed portal | Done |
| F5 | 2026-04-03 | `/host/earnings` — Shows "Something went wrong"; LTR layout with Arabic text | AMS | Resolved in upstream codebase | Done |
| F6 | 2026-04-03 | Sitewide — Back arrow points wrong direction in RTL | AMS | Added `rtl:rotate-180` to all directional icons (ArrowLeft, ArrowRight, ChevronLeft, ChevronRight) | Done |
| F7 | 2026-04-03 | Sitewide — LogOut icon points wrong direction in RTL | AMS | Added `rtl:rotate-180` to LogOut icons in Sidebar, Header, HostTopNav | Done |
| F8 | 2026-04-03 | `/host` — "Total Properties" card empty | AMS | Fixed: mapped nested API response (`properties.total`, `earnings.total`, etc.) to flat stats | Done |
| F9 | 2026-04-03 | `/listings/[id]` — Sticky price box overlaps BNPL widget on scroll | AMS | Moved BNPL inside sticky BookingWidget; removed duplicate from page | Done |
| F10 | 2026-04-03 | `/booking/[id]` — "Book for N nights" redirects to 404 when not logged in | AMS | Fixed redirect from `/auth/login` to `/auth/guest/login` | Done |
| F11 | 2026-04-03 | `/contact` — Email accepted without proper domain (e.g. word@word) | AMS | Added regex validation requiring domain TLD | Done |
| F12 | 2026-04-03 | `/` — Calendar pinned on screen, scrolls with page instead of search bar | AMS | Moved calendar from fixed portal to absolute inside search container | Done |
| F13 | 2026-04-03 | `/` — Selecting date dismisses calendar prematurely | AMS | Calendar now stays open; only closes on click outside or search | Done |
| F14 | 2026-04-03 | `/host/listings/[id]/edit` — City dropdown always shows "Select city" | AMS | Added case-insensitive + Arabic matching for city values from API | Done |
| F15 | 2026-04-03 | `/` — Font Rubik not loading reliably | AMS | Added Google Fonts link tag in layout.tsx head for reliable loading | Done |
| F16 | 2026-04-04 | `/host/settings` — Delete account does not actually delete; still logged in as host with listings | AMS | Frontend wired to authApi.deleteAccount(); backend DELETE /auth/account endpoint added — revokes tokens, deletes user, clears cookies | Done |
| F17 | 2026-04-04 | `/host/earnings` — Shows error page | AMS | Added graceful fallback with flexible field mapping | Done |
| F18 | 2026-04-04 | `/auth` — Text arrow "←" points wrong direction in Arabic (hardcoded character, not icon) | AMS | Replaced "←" with ArrowLeft icon + rtl:rotate-180 | Done |
| F19 | 2026-04-04 | `/listings/[id]` — "10% Discount" not translated to Arabic (hardcoded English) | AMS | Added isAr conditional: "خصم X%" | Done |
| F20 | 2026-04-04 | `/listings/[id]` — Property type, name, and location not translated to Arabic | AMS | Pass language to getPropertyTypeLabel; breadcrumb city uses CITIES constant | Done |
| F21 | 2026-04-04 | `/listings/[id]` — Breadcrumb city and property name not translated | AMS | City uses CITIES ar lookup; "Host since", check-in/out, approximate location all translated | Done |
| F22 | 2026-04-04 | `/listings` — Calendar pinned to screen, not relative to search bar | AMS | Replaced createPortal/fixed with absolute positioning inside search container | Done |
| F23 | 2026-04-04 | `/` and `/listings` — Calendar month names not in Arabic when language is Arabic | AMS | Added MONTH_NAMES_AR + formatMonthYear helper in MiniCalendar | Done |
| F24 | 2026-04-04 | `/` — Arabic subtitle text appears squeezed, needs spacing | AMS | Changed leading-[1.1] to leading-[1.3] for Arabic | Done |
| F25 | 2026-04-04 | `/privacy` and `/terms` — URL slugs not descriptive; RTL arrow broken | AMS | Created /privacy-policy and /terms-of-use routes; added redirects in next.config.js; fixed RTL arrow with rtl:rotate-180 | Done |
| F26 | 2026-04-04 | `/dashboard/messages` — Block User menu not displaying properly in Arabic RTL | AMS | Changed right-0 → end-0, text-left → text-start for RTL-aware positioning | Done |
| F27 | 2026-04-04 | Sitewide — SAR symbol after number instead of before; color doesn't match text | AMS | Rewrote SarSymbol from Image to inline SVG with fill="currentColor"; flipped to symbol-before-number everywhere | Done |
| F28 | 2026-04-04 | Footer — Links to /terms and /privacy instead of /terms-of-use and /privacy-policy | AMS | Updated Footer.tsx hrefs | Done |
| F29 | 2026-04-04 | `/booking/[id]` — Link to /terms instead of /terms-of-use | AMS | Updated terms link in booking page | Done |
| F30 | 2026-04-04 | `/listings` — Type filter shows emoji icons that don't match design | AMS | Removed icon field from PROPERTY_TYPES and icon render from grid buttons | Done |
| F31 | 2026-04-04 | `/listings` — City dropdown only shows selected city on reopen | AMS | Clear citySearch on focus so filteredCities shows all; restore label on close; translate on language switch | Done |
| F32 | 2026-04-04 | `/listings` — Confirm button on wrong side in Arabic (RTL) | AMS | Changed `ltr:justify-end rtl:justify-start` to `justify-end` (right in LTR, left in RTL) | Done |
| F33 | 2026-04-04 | `/listings/[id]` — BNPL widget price text wraps incorrectly in RTL | AMS | Changed from formatPrice to formatPriceNumber + SarSymbol inside `dir="ltr"` spans | Done |
| F34 | 2026-04-04 | `/dashboard/settings` — "Profile updated" toast misleading (only name changes) | AMS | Changed toast to "Name updated" and button to "Save Name" | Done |
| F35 | 2026-04-04 | `/dashboard/settings` — Change password UI present but non-functional | AMS | Removed entire change password section (state, handler, UI) | Done |
| F36 | 2026-04-04 | Sitewide — Price "/night" text forced LTR along with number | AMS | Moved `dir="ltr"` from outer container to individual price spans; "/night" label now flows with document direction | Done |
| F37 | 2026-04-04 | Sitewide — Auth redirect loses original page after login | AMS | Middleware now passes `redirect` query param with original pathname+search; auth page already reads and uses it | Done |
| F38 | 2026-04-04 | `/dashboard/support` — Support tickets not loading (API path mismatch) | AMS | Changed `api.get('/support/my-tickets')` to `api.get('/support')` to match backend route | Done |
| F39 | 2026-04-04 | `/` — City dropdown same reopen bug as /listings | AMS | Same fix: clear citySearch on focus, restore label on close, translate on language switch | Done |
| F40 | 2026-04-04 | Header — Sign out icon bright red, too prominent | AMS | Changed from `text-red-500` to `text-gray-400 hover:text-gray-600` | Done |
| F41 | 2026-04-04 | `/dashboard/messages` — Send button arrow doesn't flip in RTL | AMS | Added `rtl:rotate-180` class to send icon | Done |
| F42 | 2026-04-04 | `/listings/[id]` — SAR symbol missing from strikethrough old price | AMS | Added SarSymbol inside line-through spans for PropertyCard and BookingWidget | Done |
| F43 | 2026-04-04 | Sitewide — SAR symbol height doesn't match digit height | AMS | Set SarSymbol to `height: 0.72em` (cap-height) with `verticalAlign: -0.05em` for baseline alignment | Done |
| F44 | 2026-04-04 | `/listings` and `/` — MiniCalendar confirm button on wrong side in Arabic | AMS | Changed to `justify-end` for correct side in both LTR and RTL | Done |
| F45 | 2026-04-04 | `/dashboard/messages` — Send icon points down-left in Arabic (rotate-180 wrong) | AMS | Changed `rtl:rotate-180` to `rtl:-scale-x-100` (horizontal flip: ↗ becomes ↖) | Done |
| F46 | 2026-04-04 | `/dashboard/support/[id]` — Send icon not flipped for Arabic | AMS | Added `rtl:-scale-x-100` to Send icon | Done |
| F47 | 2026-04-04 | `/booking/[id]` — Back to property arrow not flipped in Arabic | AMS | Changed `rotate-180` to `ltr:rotate-180` on ChevronRight | Done |
| F48 | 2026-04-04 | `/listings` — Area filter bubble missing m² unit in inactive label | AMS | Inactive label now shows `Area (m²)` / `المساحة (m²)` | Done |
| F49 | 2026-04-04 | `/listings/[id]` — Guest count not carried from listings search | AMS | PropertyCard passes adults/children in URL; property detail reads and passes to BookingWidget | Done |
| F50 | 2026-04-04 | `/booking/[id]` — Editing dates/guests loses all data when going back | AMS | Edit links and back-to-property link now preserve checkIn, checkOut, adults in URL params | Done |
| F51 | 2026-04-04 | Sitewide — Arabic night plural incorrect (ليالي used for all plural) | AMS | Created `getNightLabel(count, lang)`: 1–2→ليلة, 3–10→ليالي, 11+→ليلة. Applied in BookingWidget, booking page, HeroSearch, listings, PropertyForm | Done |
| F52 | 2026-04-04 | Sitewide — Wishlist add/remove toast shows English in Arabic mode | AMS | Translated all 3 toast messages in PropertyCard (saved, removed, sign-in required, error) | Done |
| F53 | 2026-04-04 | `/dashboard/support/[id]` — All labels hardcoded English (Category, Priority, Status, etc.) | AMS | Added bilingual status/category/priority labels, translated back link, sender name, placeholder, closed message, loading text | Done |
| F54 | 2026-04-04 | `/listings/[id]` — City and district under title show English in Arabic | AMS | City uses CITIES Arabic lookup; district uses DISTRICTS Arabic lookup | Done |
| F55 | 2026-04-04 | `/dashboard/support/[id]` — Shows Header and Footer inside dashboard layout | AMS | Removed Header/Footer; page now uses dashboard layout only | Done |
| F56 | 2026-04-04 | `/` — Search step indicator shows 3 color steps instead of 4 | AMS | Added `'type'` as distinct SearchStep; steps now location→type→dates→ready with unique keys | Done |
| F57 | 2026-04-04 | `/` — Arabic subtitle text squeezed on desktop | AMS | Subtitle uses `leading-loose md:leading-[2]` for Arabic, `leading-relaxed` for English | Done |
| F58 | 2026-04-04 | Sitewide — Session expires every ~15 minutes | AMS | Refresh cookie path was `/api/auth` but routes mounted at `/api/v1/auth`; browser never sent cookie. Fixed to `/api/v1/auth` | Done |
| F59 | 2026-04-05 | Sitewide — OTP login sessions also expire after 15 min (same F58 bug) | AMS | otpController.js refresh cookie path was `/api/auth`; fixed to `/api/v1/auth` | Done |
| F60 | 2026-04-05 | `/auth` — Phone field middle area not clickable (icon blocks focus) | AMS | Added `pointer-events-none` to left/right icon wrappers in Input component | Done |
| F61 | 2026-04-05 | Sitewide — Arabic night plural incorrect (2 nights shows "2 ليلة" instead of "ليلتان") | AMS | `getNightLabel` now returns full string: 1→"1 ليلة", 2→"ليلتان" (no number), 3–10→"N ليالي", 11+→"N ليلة". All 6 call sites updated | Done |
| F62 | 2026-04-05 | Header — Mobile sign-out button still red (missed in F40) | AMS | Changed from `text-red-500` to `text-gray-500` matching desktop style | Done |
| F63 | 2026-04-05 | `/` — Date picker misaligned (left-aligned instead of centered under search box) | AMS | Changed from `ltr:left-0 rtl:right-0` to `left-1/2 -translate-x-1/2` for centered positioning | Done |
| F64 | 2026-04-05 | `/` — Arabic subtitle still squeezed (F24/F57 insufficient) | AMS | Increased to `leading-[2.2]`, widened to `max-w-2xl`, changed from `font-light` to `font-normal` for Arabic | Done |
| F65 | 2026-04-05 | `/host/messages` and `/dashboard/messages` — Timestamps show 24h format instead of AM/PM | AMS | Timestamps now use `ar-SA` locale (ص/م) for Arabic and `en-US` (AM/PM) for English with `hour12: true` | Done |
| F66 | 2026-04-05 | `/host/messages` and `/dashboard/messages` — Action menu doesn't close on outside click | AMS | Added `useRef` + `mousedown` listener to dismiss menu when clicking outside | Done |
| F67 | 2026-04-05 | `/listings` — Price filter bubble shows raw "SAR" text, no symbol or LTR wrapping | AMS | FilterBubble `label` changed to ReactNode; price bubble uses `<SarSymbol />` with `dir="ltr"` wrapping | Done |
| F68 | 2026-04-05 | `/listings` — Clearing filters doesn't refresh search results (stale state) | AMS | "Clear all" now navigates directly to `/listings` with only base search params, bypassing stale setState | Done |
| F69 | 2026-04-05 | `/booking/[id]` — Children count hardcoded to 0 in booking API call | AMS | Now reads `adults` and `children` separately from URL; API sends actual counts | Done |
| F70 | 2026-04-05 | `/booking/[id]` — Guest display shows total only, no adults/children split | AMS | Shows "2 adults, 1 child" / "2 بالغين، 1 طفل" with split counts | Done |
| F71 | 2026-04-05 | `/booking/[id]` — Back/edit links lose children count when navigating | AMS | All 3 navigation links now pass both `adults` and `children` URL params | Done |
| F72 | 2026-04-05 | `/host/listings/new` — Image upload accepts all image types (HEIC, BMP fail on backend) | AMS | Restricted to `.jpg,.jpeg,.png`; client-side type validation before upload; clear error messages | Done |
| F73 | 2026-04-05 | Sidebar — Logout button permanently red, too prominent | AMS | Changed from always-red (`text-red-500 hover:bg-red-50`) to gray default with red on hover only (`text-gray-600 hover:text-red-500`) | Done |
| F74 | 2026-04-05 | `/` — Homepage search does not save city to cookie | AMS | HeroSearch now imports `getSearchCookies` and restores city, type, dates from cookie on mount | Done |
| F75 | 2026-04-05 | `/` — Date picker in Arabic stays left-aligned instead of flipping under dates field | AMS | Changed `left-1/2 -translate-x-1/2` to `ltr:right-0 rtl:left-0` so calendar anchors near dates column in both directions | Done |
| F76 | 2026-04-05 | Sitewide — Arabic 1 night shows "1 ليلة" instead of "ليلة" | AMS | `getNightLabel` 1-night Arabic case now returns `'ليلة'` without number prefix; English unchanged | Done |
| F77 | 2026-04-05 | Sitewide — Search cookies incomplete: `/` saves nothing, `/listings` missing guests, `/listings/[id]` saves nothing, `/booking/[id]` saves nothing | AMS | `/` now restores from cookie on mount and saves on search; `/listings` already saves adults+children (verified); `/listings/[id]` saves to cookie via useEffect; `/booking/[id]` saves to cookie via useEffect; `saveSearchCookies` now merges with existing cookie so partial saves don't wipe guest data. Cleared after booking success (already in payment-callback) | Done |
| F78 | 2026-04-05 | `/listings` — Price filter slider shows "SAR" text instead of SarSymbol SVG | AMS | Replaced "SAR" text with `<SarSymbol />` component in all 3 slider labels; added `dir="ltr"` wrapping | Done |
| F79 | 2026-04-05 | `/booking/[id]` — Going back to `/listings/[id]` loses adults/children split, shows total as adults | AMS | BookingWidget now sends `adults` and `children` as separate URL params instead of combined `guests` total | Done |
| F80 | 2026-04-05 | `/host/messages` — Send icon not flipped horizontally in Arabic | AMS | Added `rtl:-scale-x-100` to Send icon, matching `/dashboard/messages` fix (F45) | Done |
| F81 | 2026-04-05 | `/host/listings/new` — Image upload fails with "Image upload failed. Please try again." | AMS | Backend `.env` missing Cloudinary credentials (`CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`). Not a code bug — needs credentials from Cloudinary account | Open |
| F82 | 2026-04-06 | `/listings` — Guest details (adults/children) not recorded in cookies | AMS | `guests` derived from `adults+children`; `handleSearch` and cookies now save both separately | Done |
| F83 | 2026-04-06 | `/listings` — Going back from `/listings/[id]` combines children with adults as `&guests=3` | AMS | URL now uses `adults` and `children` params instead of combined `guests`; init reads `adults`/`children` from URL or cookie | Done |
| F84 | 2026-04-06 | `/listings` — Arabic night label (ليلة/ليلتان) missing space before word, shows connected to previous word | AMS | Added non-breaking space (`\u00A0`) before all Arabic night labels in `getNightLabel` | Done |
| F85 | 2026-04-06 | `/listings` — Area filter slider unit flipped in Arabic, should stay on the right always | AMS | Added `dir="ltr"` to area slider value label, range input, and scale labels container | Done |
| F86 | 2026-04-06 | `/listings` — SAR symbol in price slider should be on the left of the number | AMS | Flipped from `{number} <SarSymbol />` to `<SarSymbol /> {number}`; restructured to current value on top, scale labels below slider | Done |
| F87 | 2026-04-06 | `/listings` — Clear all button does nothing when filters are applied, should auto-search | AMS | `clearAllFilters` increments `autoSearch` counter; `useEffect` re-fetches on change | Done |
| F88 | 2026-04-06 | `/listings` — Canceling a single filter does not auto-search, requires manual search click | AMS | All 9 filter `onClear` handlers now call `setAutoSearch((n) => n + 1)` to trigger auto-fetch | Done |
| F89 | 2026-04-06 | `/listings` — Price filter icon shows dollar sign, should be SAR symbol | AMS | Replaced `DollarSign` (lucide) with `SarSymbol` component as FilterBubble icon; removed unused import | Done |
| F90 | 2026-04-06 | `/listings` — Price slider should follow page direction (RTL in Arabic, LTR in English), only number+symbol wrapped in dir=ltr | AMS | Removed `dir="ltr"` from slider input and labels container; kept `dir="ltr"` only on individual number+symbol spans | Done |
| F91 | 2026-04-06 | `/listings` — URL should not contain search/filter details, rely on cookies only | AMS | Removed all URL params from `handleSearch` and clear-all; navigate to clean `/listings`; all state reads from cookies via `useEffect` on mount; removed `useSearchParams` | Done |
| F92 | 2026-04-06 | `/listings` — Guest picker does not remember/restore adults and children count | AMS | State initializes empty then restores from cookies via `useEffect` on client mount (fixes SSR null `document` issue); adults and children restored from cookie | Done |
| F93 | 2026-04-06 | `/listings` — Guest button shows "Guests" text even with 1 adult selected | AMS | Changed condition from `adults + children > 1` to `adults >= 1` so 1 adult shows "1" instead of empty "Guests" | Done |
| F94 | 2026-04-06 | `/listings` — Area filter slider should treat number+unit like F90 (only number+unit in dir=ltr, not whole slider) | AMS | Removed `dir="ltr"` from slider input and labels container; added `dir="ltr"` only on individual number+unit `<span>` elements so slider follows page direction | Done |
| F95 | 2026-04-06 | `/listings` — Type filter should auto-update on click without Apply button | AMS | Each type click triggers `toggleType` + `setAutoSearch` for instant re-fetch; removed Apply button from type filter popover | Done |
| F96 | 2026-04-06 | `/listings` — Selecting multiple types in filter returns no results (backend exact-matches comma string instead of `$in`) | AMS | Frontend sends `type` as array (`type[]=chalet&type[]=villa`) instead of comma-joined string; backend receives array which MongoDB matches via implicit `$in`. Backend also has `$in` fallback for comma strings | Done |
| F97 | 2026-04-06 | `/listings` — Price filter: dismissing by clicking away should auto-apply (same as Apply button) | AMS | useEffect on `openFilter` detects transition from `'price'` to `null` and triggers autoSearch; removed Apply button | Done |
| F98 | 2026-04-06 | `/listings` — Area filter: dismissing by clicking away should auto-apply (same as Apply button) | AMS | Same useEffect handles `'area'` to `null` transition; removed Apply button | Done |
| F99 | 2026-04-06 | `/listings` — Bedrooms filter reads stale state, doesn't actually filter | AMS | Replaced `handleSearch()` with `setAutoSearch((n) => n + 1)` so fetch runs after state updates | Done |
| F100 | 2026-04-06 | `/listings` — Rating filter reads stale state, doesn't actually filter | AMS | Same fix — `setAutoSearch` instead of `handleSearch` | Done |
| F101 | 2026-04-06 | `/listings` — Pool toggle reads stale state, doesn't actually filter | AMS | Same fix — `setAutoSearch` instead of `setTimeout(handleSearch, 0)` | Done |
| F102 | 2026-04-06 | `/listings` — Offers toggle reads stale state, doesn't actually filter | AMS | Same fix — `setAutoSearch` instead of `setTimeout(handleSearch, 0)` | Done |
| F103 | 2026-04-06 | `/listings` — Direction filter reads stale state, doesn't actually filter | AMS | Same fix — `setAutoSearch` instead of `handleSearch` | Done |
| F104 | 2026-04-06 | `/listings` — District filter reads stale state, doesn't actually filter | AMS | Same fix — `setAutoSearch` instead of `handleSearch` | Done |
| F105 | 2026-04-06 | `/listings/[id]` and `/booking/[id]` — URLs should not contain search params; read from cookies only | AMS | PropertyCard links to clean `/listings/[id]`; detail page reads dates/guests from cookies via `getSearchCookies`; BookingWidget saves to cookie then navigates to clean `/booking/[id]`; booking page reads from cookies; auth redirect uses clean URL | Done |
| F106 | 2026-04-06 | `/listings/[id]` — BookingWidget calendar month names not in Arabic when language is Arabic | AMS | Pass `locale={language}` to MiniCalendar in BookingWidget; month names now use MONTH_NAMES_AR | Done |
| F107 | 2026-04-06 | `/listings/[id]` — BookingWidget calendar shows duplicate month header (one in nav bar, one in MonthGrid) | AMS | Added `showHeader` prop to MonthGrid; non-dual mode passes `showHeader={false}` since nav bar already shows month; dual mode keeps both headers | Done |
| F108 | 2026-04-06 | `/listings/[id]` — Check-in/check-out date boxes show English dates when language is Arabic | AMS | Uses `toLocaleDateString('ar-SA')` for Arabic; keeps `format(date, 'MMM d, yyyy')` for English | Done |
| F109 | 2026-04-06 | `/listings/[id]` — Changing dates in BookingWidget does not update cookies (navigating to /listings shows old dates) | AMS | Added `useEffect` watching `checkIn`/`checkOut`/`adults`/`children` that calls `saveSearchCookies` on change (skips initial mount) | Done |
| F110 | 2026-04-06 | `/listings/[id]` — Changing guest count in BookingWidget does not update cookies (navigating to /listings shows old count) | AMS | Same useEffect handles guest changes — cookies always in sync | Done |
| F111 | 2026-04-06 | `/booking/[id]` — Edit buttons for dates/guests link to `/listings/[id]` with URL params; should be clean URL | AMS | Replaced all 3 edit links with clean `/listings/[id]` — data already in cookies | Done |
| F112 | 2026-04-06 | `/booking/[id]` — "Property not available for selected dates" error needs better explanation | AMS | Changed to "This property is already booked for one or more of your selected dates. Please choose different dates." (bilingual) | Done |
| F113 | 2026-04-06 | `/booking/[id]` — Dates displayed in English format when language is Arabic | AMS | Uses `toLocaleDateString('ar-SA')` for Arabic date display | Done |
| F114 | 2026-04-06 | `/contact` — Back arrow "←" points wrong direction in Arabic | AMS | Replaced hardcoded "←" with ArrowLeft icon + `rtl:rotate-180` | Done |
| F115 | 2026-04-06 | `/booking/[id]` — City under property name shows in English when language is Arabic | AMS | Added CITIES import; lookup Arabic city name via `CITIES.find()` | Done |
| F116 | 2026-04-06 | Sitewide — Arabic guest count grammar incorrect (ضيف/ضيفان/ضيوف rules) | AMS | New `getGuestLabel(count, lang)` in utils.ts: 1=ضيف, 2=ضيفان, 3-10=ضيوف, 11+=ضيف. Applied in PropertyCard, SearchFilters, BookingWidget, booking page | Done |
| F117 | 2026-04-06 | Sitewide — Arabic adult/children count grammar incorrect (بالغ/بالغان/بالغين and طفل/طفلان/أطفال rules) | AMS | New `getAdultLabel` and `getChildLabel` in utils.ts with full Arabic plural rules. Applied in booking page, listings page guest picker | Done |
| F118 | 2026-04-06 | Sitewide — Arabic separator between adults and children should be "و" not "،" or "·" | AMS | Changed `،` / `·` to `و` in booking page and listings page guest display | Done |
| F119 | 2026-04-06 | `/booking/[id]` — "Visa, Mastercard, mada" not translated in Arabic | AMS | Added bilingual: `فيزا، ماستركارد، مدى` / `Visa, Mastercard, mada` | Done |
| F120 | 2026-04-06 | Sitewide — "SAR" text shown instead of SAR symbol (﷼) | AMS | Replaced all `SAR` / `ر.س` text with `<SarSymbol />` component across dashboard, host, admin, earnings, bookings, balance, listings, and PropertyForm pages | Done |
| F121 | 2026-04-06 | `/` — "Browse by Type" links pass URL params to `/listings` but params are ignored (cookie-only architecture) | AMS | Changed from `<Link href="/listings?type=...">` to `<button>` with `clearSearchCookies` + `saveSearchCookies({ type })` + `router.push('/listings')` | Done |
| F122 | 2026-04-06 | `/` — City bubbles under search bar pass URL params to `/listings` but params are ignored | AMS | Changed from `router.push('/listings?city=...')` to `clearSearchCookies` + `saveSearchCookies({ city, checkIn, checkOut })` + `router.push('/listings')` | Done |
| F123 | 2026-04-06 | `/listings` — Properties with minNights > selected nights still shown in results | AMS | Added client-side filter after fetch: removes properties where `rules.minNights > selectedNights` | Done |
| F124 | 2026-04-06 | `/listings/[id]` — BookingWidget has no early warning for minimum nights; user only finds out at booking page | AMS | Added inline amber warning below date/guest section when `nights < minNights`; added validation in `handleBookNow` before navigation | Done |
| F125 | 2026-04-06 | `/booking/[id]` — MIN_STAY and MAX_STAY error messages show English in Arabic mode | AMS | Replaced hardcoded "nights"/"ليالي" with `getNightLabel()` for proper Arabic plural grammar | Done |
| F126 | 2026-04-06 | `/booking/[id]` — Tabby and Tamara BNPL descriptions show "SAR" text from `formatPrice()` | AMS | Replaced `formatPrice()` with `<SarSymbol /> formatPriceNumber()` in both Tabby and Tamara installment descriptions | Done |
| F127 | 2026-04-07 | `/` — Search bar button navigates with URL params (`?city=...`) instead of cookies | AMS | Changed `handleSearch` to save cookies only and navigate to clean `/listings` | Done |
| F128 | 2026-04-07 | `/` — City bubbles clear guests/type/dates when navigating to `/listings` | AMS | Removed `clearSearchCookies()`; `saveSearchCookies` merges city into existing cookies, preserving dates/guests/type | Done |
| F129 | 2026-04-07 | `/` — "Browse by Type" clears city/dates/guests when navigating to `/listings` | AMS | Removed `clearSearchCookies()`; `saveSearchCookies` merges type into existing cookies, preserving city/dates/guests | Done |
| F130 | 2026-04-07 | `/listings` — Navigating from homepage doesn't auto-search (must click search manually) | AMS | Added `ready` flag; `fetchProperties` only runs after cookies are restored to state; ensures first fetch uses correct cookie values | Done |
| I56 | 2026-04-07 | `/admin/bookings` — Add ability to delete bookings | AMS | Backend `DELETE /admin/bookings/:id` with ActivityLog; frontend delete button per row with confirmation dialog; `held` status badge added | Done |
| F131 | 2026-04-07 | `/listings` — Heart/favorite button not working; click navigates to property page instead | AMS | Added `e.stopPropagation()` to prevent Next.js Link from capturing click; made toggle optimistic (instant UI feedback) | Done |
| F132 | 2026-04-07 | `/listings` — Wishlist not persisting after page refresh; heart resets to gray | AMS | `getMe` returns populated wishlist objects — normalized to string IDs; `toggleWishlist` made optimistic (don't rely on API response); PropertyCard syncs with context via `useEffect` | Done |
| F133 | 2026-04-07 | `/booking/[id]` — "Back to review" arrow points forward in Arabic RTL | AMS | Changed `←` to `→` for Arabic text | Done |
| F134 | 2026-04-07 | `/dashboard/support/[id]` — Sending reply shows "Failed to send reply" | AMS | Vercel proxy issue; made reply optimistic (add to UI immediately, reload on response) | Done |
| F135 | 2026-04-07 | `/dashboard/support/[id]` — Timestamp shows seconds (e.g. 11:05:23 AM) | AMS | Formatted with `hour: '2-digit', minute: '2-digit'` — no seconds | Done |
| F136 | 2026-04-07 | `/dashboard/support/[id]` — AM/PM shown in English when language is Arabic | AMS | Pass `ar-SA` locale to `toLocaleString()` | Done |
| I57 | 2026-04-07 | `/dashboard/bookings` — Clicking booking goes to `/booking/[id]` (checkout page) and redirects away | AMS | Created dedicated `/dashboard/bookings/[id]` detail page with property card, stay details, pricing breakdown, payment & booking status | Done |
| F138 | 2026-04-07 | Site-wide — Arabic numerals (٠١٢) shown instead of Western (012) | AMS | Replaced `ar-SA` locale with `ar-u-nu-latn` (BCP 47 Latin numeral extension) across 32 files; added `'en'` locale to bare `.toLocaleString()` calls | Done |
| I58 | 2026-04-07 | `/dashboard/settings` — Page layout messy, everything crammed in one card | AMS | Redesigned with separate sections: account overview (avatar, role, member since), personal info (name + save), contact info (email/phone with dividers), become-a-host gradient card | Done |
| F139 | 2026-04-07 | `/dashboard/bookings/[id]` — City and district shown in English when language is Arabic | AMS | Added `translateCity` / `translateDistrict` using CITIES/DISTRICTS constants from `@/lib/constants` | Done |
| F140 | 2026-04-07 | `/dashboard/settings` — Email OTP not received after editing email | AMS | Made OTP flow optimistic (show code input immediately); root cause: SMTP not configured on backend — emails only logged to console. Requires SMTP_HOST/USER/PASS env vars | Done |
| I59 | 2026-04-08 | Dashboard sidebar — No visual indicator for unread bookings, messages, or support replies | AMS | Backend: `GET /notifications/unread-summary` aggregates unread counts by category. Frontend: Sidebar fetches counts every 30s; red badge with number on Bookings/Messages/Support icons; clears on navigation. Works for guest + host dashboards | Done |

## Improvements

| # | Date | Description | Author | Remark | Status |
|---|------|-------------|--------|--------|--------|
| I1 | 2026-04-03 | `/auth/guest/login` — Auto-focus on phone field on load | AMS | Added autoFocus and useRef | Done |
| I2 | 2026-04-03 | `/auth/guest/login` — Auto-focus on OTP field after sending code | AMS | Added useEffect + useRef to focus OTP input | Done |
| I3 | 2026-04-03 | `/host/listings/new` — City should be dropdown, not free-text | AMS | Replaced text input with `<select>` using CITIES constant on new + edit pages | Done |
| I4 | 2026-04-03 | `/` — Use font Rubik as default for the website | AMS | Replaced Inter with Rubik in globals.css, tailwind.config, LanguageContext | Done |
| I5 | 2026-04-03 | `/` — Search bar width too short | AMS | Widened from `max-w-4xl` (896px) to `max-w-5xl` (1024px) | Done |
| I6 | 2026-04-03 | `/` — Dual calendar view on desktop | AMS | Added `dual` prop to MiniCalendar; shows two months side-by-side on md+ screens | Done |
| I7 | 2026-04-03 | `/host/settings` — Add ability to delete account and listings | AMS | Added delete account UI with confirmation dialog; logs out user on confirm | Done |
| I8 | 2026-04-03 | Sitewide — Language toggle on dashboards | AMS | Already present in HostTopNav in upstream | Done |
| I9 | 2026-04-03 | `/listings` — Search bar dropdowns should match homepage style | AMS | Re-applied: searchable city dropdown matching homepage style | Done |
| I10 | 2026-04-03 | `/listings` — Date picker should display calendar like homepage | AMS | Re-applied: MiniCalendar portal with dual view on desktop | Done |
| I11 | 2026-04-03 | `/listings` — Guests should be picker with +/- for adults and kids | AMS | Re-applied: guest picker with +/- buttons for adults and children | Done |
| I12 | 2026-04-03 | `/contact` — Subject field should be required | AMS | Added `*` label and validation check | Done |
| I13 | 2026-04-03 | `/` — City select should auto-open property type dropdown | AMS | handleCitySelect now opens type dropdown automatically | Done |
| I14 | 2026-04-03 | `/` — Property type select should auto-open calendar | AMS | Type selection now opens calendar automatically | Done |
| I15 | 2026-04-04 | `/host` — No way to change language on host dashboard | AMS | Added language toggle (Globe icon) to Sidebar — works on guest + host + admin dashboards | Done |
| I16 | 2026-04-04 | `/auth` — Merge guest/host login into one page; system determines user type | AMS | Unified phone+OTP page; old guest/host routes redirect to /auth; role auto-detected from backend response | Done |
| I17 | 2026-04-04 | `/auth` — Card area outside input fields should be unclickable/unselectable | AMS | Added select-none to form card | Done |
| I18 | 2026-04-04 | `/auth` — Country code picker with flag icon; fix OTP for international codes | AMS | GCC country picker (SA/UAE/BH/KW/OM/QA) with flags; countryCode sent to API; backend support needed from TAK | Done |
| I19 | 2026-04-04 | `/dashboard` — Cards: reservations, wallet balance, hosts blocks count, hosts rating | AMS | 4 stat cards: Reservations, Balance (linked), Host Blocks, My Rating; balance/blocks/rating fields added to User type | Done |
| I20 | 2026-04-04 | `/dashboard` — Add balance page in sidebar | AMS | New `/dashboard/balance` page with balance card + transaction history; Wallet nav item added to sidebar | Done |
| I21 | 2026-04-04 | `/dashboard` — Add favorites page in sidebar | AMS | New `/dashboard/favorites` page showing wishlisted properties; Heart nav item added to sidebar | Done |
| I22 | 2026-04-04 | `/dashboard/settings` — Email field should be editable with email verification flow | AMS | Email now has Edit button → enter new email → send code → verify code; backend updateProfile extended with email verification (generates 6-digit code, stores hashed, verifies on confirm) | Done |
| I23 | 2026-04-04 | `/listings` — Search bar auto-flow: city → dates → guests; OK button to advance fields | AMS | City select auto-opens calendar; calendar OK opens guest picker; guest picker has OK button | Done |
| I24 | 2026-04-04 | `/listings` — Add filter by area | AMS | Area/district text filter bubble with popover input | Done |
| I25 | 2026-04-04 | `/listings` — Add filter by discount/offers | AMS | Offers toggle bubble; one-click to filter discounted properties | Done |
| I26 | 2026-04-04 | `/listings` — Add filter by ratings | AMS | Rating bubble with 3+/4+/4.5+ quick-select options | Done |
| I27 | 2026-04-04 | `/listings` — Add filter by number of bedrooms | AMS | Bedrooms bubble with 1+ through 5+ quick-select options | Done |
| I28 | 2026-04-04 | `/listings` — Filter Unit Type: allow multiple selection | AMS | Type bubble opens grid of property types; multi-select with Apply button | Done |
| I29 | 2026-04-04 | `/listings` — Filter as icon bubbles; clicking opens pop card with filter settings | AMS | All filters as pill bubbles with icons; click opens popover card; active state + clear button | Done |
| I30 | 2026-04-04 | `/listings/[id]` — BNPL widget: show full payment info; click to expand options | AMS | Compact header with expand/collapse; shows timeline + providers on click | Done |
| I31 | 2026-04-04 | `/listings/[id]` — Add ability to chat with host | AMS | Added "Message" button on property page linking to dashboard messages; replaced host messages placeholder with full chat UI (conversations, real-time polling, block/unblock) | Done |
| I32 | 2026-04-04 | `/` and `/listings` — Add OK/submit button inside calendar to dismiss and advance to next field | AMS | Added onConfirm prop to MiniCalendar; OK button shown when both dates selected; added on both homepage and listings | Done |
| I33 | 2026-04-04 | `/blog` — Add blog page with categories, posts, and admin CMS (similar to blog.gathern.co) | AMS | Full stack: BlogPost + BlogCategory models, blog controller + routes, frontend listing + detail pages (bilingual), admin CMS, nav links in Header/Footer/Sidebar | Done |
| I34 | 2026-04-04 | `/listings` — Add Pool filter, Direction filter, Area slider (0-1500+ m²), Rating from 10, Price slider (0-4000+), District dropdown by city | AMS | New filter bubbles: Pool toggle, Direction dropdown, Area range slider, Price range slider, District city-dependent dropdown; backend extended with area/direction/pool/district query support | Done |
| I35 | 2026-04-04 | `/dashboard/settings` — Phone number change should require OTP verification | AMS | Phone section now read-only with Edit button; sends OTP to new number; verify code to apply change; backend updateProfile requires phoneVerificationCode for phone changes | Done |
| I36 | 2026-04-04 | `/listings` — Add dropdown arrows to expandable filter bubbles | AMS | Added `hasDropdown` prop to FilterBubble; ChevronDown icon on Type, Bedrooms, Rating, Price, Area, Direction, District filters | Done |
| I37 | 2026-04-04 | `/listings/[id]` — Guest picker with adults/children split | AMS | Booking widget now has separate Adults (13+, min 1) and Children (0–12, min 0) counters with +/- buttons; total respects maxGuests | Done |
| I38 | 2026-04-04 | `/` — Search step indicator showing progress through fields | AMS | Added Property step label to step indicator in HeroSearch | Done |
| I39 | 2026-04-04 | `/host/listings/new` — District should be dropdown, not free text | AMS | District is now city-dependent `<select>` using DISTRICTS constant with Arabic labels; falls back to text input if city has no predefined districts; clears on city change | Done |
| I40 | 2026-04-04 | `/host/listings/new` — Dropdown styling should match homepage | AMS | Updated inputClass to rounded-xl, bg-gray-50/50, primary ring focus matching HeroSearch style | Done |
| I41 | 2026-04-04 | `/host/listings/new` — Error messages need clear field-specific explanation | AMS | Lists missing required fields by name; requires at least 1 photo; surfaces Mongoose validation errors and backend error messages; image upload shows file type/size hints. All bilingual | Done |
| I42 | 2026-04-05 | Sitewide — Persist search details in cookies across pages | AMS | New `searchCookies.ts` utility; HeroSearch and listings save city/type/dates/guests to cookie; listings restores from cookie when URL empty; cleared after booking success | Done |
| I43 | 2026-04-05 | `/host/messages` and `/dashboard/messages` — Add "Report User" to action menu | AMS | New menu item with Flag icon; bilingual "إبلاغ عن المستخدم" / "Report User"; shows toast on click | Done |
| I44 | 2026-04-05 | Sitewide — Dashboard sidebar should be sticky on scroll | AMS | DashboardShell sidebar wrapper changed from `lg:static` to `lg:sticky lg:top-0 lg:h-screen`; Sidebar uses `h-full overflow-y-auto` for internal scrolling | Done |
| I45 | 2026-04-06 | `/listings` — Search bar fields (city, dates, guests) should auto-search when dropdown is dismissed | AMS | Added `prevRef` useEffects for city dropdown, calendar, and guest picker — triggers `autoSearch` + saves cookies when each closes with a value set | Done |
| I46 | 2026-04-07 | Backend — Reservation hold system: `held` status + `holdExpiresAt` field in Booking model | AMS | Added `held` to status enum, `holdExpiresAt` Date field, index for hold queries | Done |
| I47 | 2026-04-07 | Backend — `POST /bookings/hold` endpoint for 2-min reservation holds | AMS | New `createHold` controller: validates dates/capacity, cancels user's old holds (1 per user), creates hold with 2-min TTL; validation middleware `holdBookingRules` | Done |
| I48 | 2026-04-07 | Backend — `overlapQuery` includes active holds in conflict detection | AMS | Status check now uses `$and` with `$or` to match `pending/confirmed` OR `held` with non-expired `holdExpiresAt` | Done |
| I49 | 2026-04-07 | Backend — `createBooking` accepts `holdId` to convert hold → pending booking | AMS | If holdId valid and not expired, converts hold to pending (preserves pricing); if expired, cancels hold and falls through to normal creation | Done |
| I50 | 2026-04-07 | Backend — `getProperty` returns `bookedDates` from active bookings + holds | AMS | Queries Booking collection for pending/confirmed/active-held records; returns `{ start, end }` date ranges for calendar graying | Done |
| I51 | 2026-04-07 | Backend — Confirmed bookings hide property from search; holds do not | AMS | `getProperties` excludes properties with `pending/confirmed` bookings (hard lock) for selected dates; `held` (soft lock, 2 min) keeps property visible | Done |
| I52 | 2026-04-07 | Backend — `checkAvailability` includes active holds in conflict check | AMS | Same `$and`/`$or` pattern as overlapQuery for hold-aware availability checking | Done |
| I53 | 2026-04-07 | Frontend — BookingWidget creates hold on "Book Now"; blocks if dates taken | AMS | Async handleBookNow: calls `createHold` API, stores holdId in localStorage; if DATES_UNAVAILABLE shows error toast and blocks navigation; other failures proceed silently | Done |
| I54 | 2026-04-07 | Frontend — Booking page uses holdId to convert hold → booking | AMS | Reads holdId from localStorage, passes to `createBooking` API; cleans up localStorage after use; falls through to normal creation if hold expired | Done |
| I55 | 2026-04-07 | Frontend — Calendar shows booked/held dates as unavailable | AMS | BookingWidget merges `property.bookedDates` ranges into individual date strings and passes to MiniCalendar alongside `unavailableDates` | Done |
| F141 | 2026-04-07 | `/listings` — Heart/favorite icon has oversized white circle background | AMS | Removed `w-10 h-10 bg-white rounded-full shadow-md`; replaced with `p-1.5 drop-shadow` for cleaner floating icon | Done |
| I60 | 2026-04-07 | Backend — Multi-list wishlist system (like Gathern/Airbnb) | AMS | New `Wishlist` model with `user/name/isDefault/properties`; unique `{ user, name }` index; lazy `getOrCreateDefault` creates default "مفضلاتي" list and migrates existing `user.wishlist`; `wishlistController` with getLists/getList/create/update/delete/toggleProperty/moveProperty; `syncUserWishlist` keeps `user.wishlist` flat array in sync; `authController.toggleWishlist` syncs to default list | Done |
| I61 | 2026-04-07 | Frontend — Multi-list favorites page + list detail page | AMS | Rewrote `/dashboard/favorites` from flat grid to list cards (cover image, name, count); create/rename/delete lists; context menu on non-default lists; new `/dashboard/favorites/[listId]` detail page showing properties with remove button; `wishlistsApi` in `api.ts`; `WishlistList` type | Done |
| F142 | 2026-04-08 | `/listings` — Heart icon at wrong position (top instead of bottom) | AMS | Moved back to `bottom-3`; made unfilled heart semi-transparent (`fill-white/60 text-white/80`) | Done |
| F143 | 2026-04-08 | `/dashboard/favorites` — Default list "مفضلاتي" not translated to English | AMS | Added `getDisplayName()` — shows "My Favorites" in English, "مفضلاتي" in Arabic based on `list.isDefault`; applies on both list cards and detail page header | Done |
| F144 | 2026-04-08 | `/dashboard/favorites` — Broken image on list cards when coverImage is empty | AMS | Backend `getLists` was returning image object instead of URL string; fixed to `images?.[0]?.url` | Done |
| F145 | 2026-04-08 | `/dashboard/favorites` — 3-dot menu doesn't dismiss when clicking outside | AMS | Added `useRef` + `mousedown` click-outside handler; menu closes on any outside click | Done |
| I62 | 2026-04-08 | `/listings` — Single-select list picker on heart click | AMS | Heart click opens popover with all lists; tap one to save + close. "New list" creates + saves in one step. If already wishlisted, heart removes. Simplified from multi-checkbox to single-select UX | Done |
| I63 | 2026-04-08 | `/dashboard/favorites/[listId]` — Full filter bar with mismatch notes | AMS | Same filter bubbles as `/listings` (Type, Bedrooms, Rating, Pool, Offers, Price, Area, Direction, District) but client-side on loaded properties. Non-matching properties shown dimmed (50% opacity) with amber notes explaining why (e.g. "No pool", "Only 2 bedrooms"). Backend `getList` now populates `amenities/area/direction` | Done |
| I64 | 2026-04-08 | `/listings` — Wishlist picker: renamed labels, added Clear button, portal positioning | AMS | Header "Save to lists" → "Wishlist" / "المفضلة"; "New list" → "New wishlist" / "قائمة مفضلة جديدة"; new red "Clear" button removes property from ALL wishlists at once; picker now renders via `createPortal` with fixed positioning to prevent clipping by `overflow-hidden` card/image containers; closes on scroll; heart button excluded from click-outside dismiss | Done |
| I65 | 2026-04-08 | `/dashboard/favorites/[listId]` — Search cookies carry over sitewide | AMS | Favorites detail page now reads `hostn_search` cookie on mount (city, check-in/out, adults, children) and writes back when dropdowns close — same pattern as `/listings`. Search state persists across Home → Listings → Favorites navigation. `clearAllFilters` also clears cookies | Done |
| F146 | 2026-04-08 | Sitewide — District shown in English on Arabic property cards | AMS | Added `translateDistrict(district, city)` using `DISTRICTS` constant (same pattern as `translateCity`). Fixed in `PropertyCard.tsx` (listings grid) and `PropertyListCard` (favorites detail). Both now look up Arabic district name by city key | Done |
| I66 | 2026-04-08 | `/listings` + `/dashboard/favorites/[listId]` — Total price + weekly/monthly pricing on cards | AMS | PropertyCard and PropertyListCard now show total price below nightly rate when check-in/out dates are set. If stay ≥ 7 nights, displays per-week rate; ≥ 30 nights, per-month rate. Total always shows exact `effectivePrice × nights`. Dates passed as optional `checkIn`/`checkOut` props from parent pages; home page FeaturedListings unaffected (no dates) | Done |
| F147 | 2026-04-08 | `/dashboard/favorites/[listId]` — Mismatch note hard to read (white-on-amber with opacity) | AMS | Moved `opacity-50` from the grid to just the card wrapper (note stays full opacity). Upgraded note styling: `bg-amber-100` → stronger background, `border border-amber-300` → visible border, `text-xs font-medium text-amber-800` → larger darker text, bigger icon | Done |
| I67 | 2026-04-08 | Sitewide — Compact property card: icon-only capacity row + rating beside price | AMS | Replaced text labels ("4 guests", "2 beds") with icon + number only (👤4 🛏2 🛁2 📐150m²). Added bathrooms (`Bath`) and area (`Ruler`). Moved `StarRating` from top of card to right side of price row for better comparison at a glance. Applied to both `PropertyCard` and `PropertyListCard` in favorites detail | Done |
| F148 | 2026-04-08 | `/dashboard/favorites/[id]` — Mismatch note barely visible (light amber on dimmed card) | AMS | Changed from `bg-amber-100 text-amber-800` to `bg-amber-500 text-white font-semibold` — solid amber background with white text is visible regardless of card opacity | Done |
| F149 | 2026-04-08 | `/dashboard/favorites/[id]` — Date fields show English even in Arabic mode | AMS | `formatDateDisplay` was hardcoded to `'en'` locale. Now accepts locale param and uses `'ar-SA'` for Arabic. Call sites updated to pass `lang` | Done |
| F150 | 2026-04-08 | `/dashboard/favorites/[id]` — Reserved properties shown without mismatch note | AMS | Added booked-dates overlap check to `getMismatchReasons` — if selected dates overlap a property's bookings, shows "محجوز في هذه التواريخ" / "Booked for these dates". Backend `getList` now queries Booking collection and attaches `bookedDates` to each property | Done |
| F151 | 2026-04-08 | Sitewide — Adding to wishlist shows no success feedback | AMS | `handleToggleInList` in PropertyCard was missing a toast. Now shows `Saved to "list name"` / `Removed from "list name"` on every toggle. `handleCreateAndAdd` and `handleClearFromAll` already had toasts | Done |
| F152 | 2026-04-09 | `/dashboard` — Recent Bookings link goes to wrong URL | AMS | Changed `/booking/${id}` to `/dashboard/bookings/${id}` in dashboard page | Done |
| F153 | 2026-04-09 | Sitewide — Heart icon not red on hover when unfilled | AMS | Added `hover:fill-red-400 hover:text-red-400` to unfilled Heart className in PropertyCard | Done |
| F154 | 2026-04-09 | `/` — Destination dropdown missing "All cities" option | AMS | Added hardcoded "All cities" / "جميع المدن" button to HeroSearch dropdown. Also saves city to cookie on selection | Done |
| F155 | 2026-04-09 | `/` + `/listings` — Cookie not saving/clearing "All cities" correctly | AMS | `saveSearchCookies` stripped empty values, keeping old city from merged cookie. Fixed: keys explicitly passed as empty now remove the old value. Listings auto-search now triggers even when city is empty | Done |
| I68 | 2026-04-09 | Backend — Public host profile API (`GET /api/v1/hosts/:id`) | AMS | New `publicHostController.js` + `publicHost.js` route. Returns host info, aggregate rating across all properties, paginated reviews (with guest/property), paginated properties. No auth required | Done |
| I69 | 2026-04-09 | `/listings/[id]` — Share + Favorite buttons, host link, segmented nav | AMS | Added Share2 (Web Share API + clipboard fallback) and Heart (wishlist toggle) buttons to title area. Host name now links to `/hosts/[id]` with collective rating and property count. Segmented nav (Specification, Guest Reviews, Location & Map, Terms & Policies) with scroll-to-section + IntersectionObserver. Removed Message button (moved to booking detail) | Done |
| I70 | 2026-04-09 | `/hosts/[id]` — New public host profile page | AMS | Banner with avatar/name/verification, stats grid (response time, units, member since, rating), segmented tabs (Reviews/Properties). Reviews show rating stars, comment, reviewer, property name with pagination. Properties show PropertyCard grid with pagination. Share button | Done |
| I71 | 2026-04-09 | `/dashboard/bookings/[id]` — Host info + contact button | AMS | Backend now populates `property.host` in booking endpoint (nested populate). Frontend shows host avatar, name (linked to `/hosts/[id]`), verification badge, and Message button | Done |
| I72 | 2026-04-10 | Property→Unit system — Unit schema and model | AMS | New `Unit` Mongoose model (`models/Unit.js`) with 28+ fields: bilingual names, area, capacity, bedrooms/bathrooms/kitchen/pools sub-docs, 7-day pricing, amenities, features, cancellation, deposit/insurance, images, ratings, unavailableDates. Indexes on `{ property, isActive }` and `{ property, createdAt }` | Done |
| I73 | 2026-04-10 | Property→Unit system — Backend CRUD for units | AMS | New `unitController.js` with 7 endpoints: createUnit, getUnits, getMyPropertyUnits, getUnit, updateUnit, deleteUnit (soft), duplicateUnit. Whitelist sanitization (ALLOWED_UNIT_FIELDS). New `routes/units.js` with `mergeParams: true`; dual mount in server.js at `/properties/:propertyId/units` and `/units` | Done |
| I74 | 2026-04-10 | Property→Unit system — Frontend API client for units | AMS | Added `unitsApi` to `api.ts` with: getForProperty, getManage, getOne, create, update, remove, duplicate | Done |
| I75 | 2026-04-10 | Property→Unit system — Host dashboard unit management UI | AMS | New pages: `/host/listings/[id]/units` (unit list with grid cards, status toggle, duplicate), `/host/listings/[id]/units/new` (create), `/host/listings/[id]/units/[unitId]/edit` (edit). Shared `UnitForm.tsx` component (~500 lines, 10 sections, 37 amenities, bilingual enums). "Units" link added to host listings page | Done |
| I76 | 2026-04-10 | Property→Unit system — Public unit display on property page | AMS | New `UnitsList.tsx` component with accordion-style cards showing unit details (description, pricing table, amenities, pools, galleries). Self-hides when property has no units. Added to property detail Specification tab | Done |
| I77 | 2026-04-10 | Property→Unit system — Booking integration with units | AMS | Backend: `Booking.unit` field, `calculateUnitPricing` (per-day-of-week), unit validation in createHold/createBooking, unit-level overlap queries, unit populate on booking queries. Frontend: unit selector in BookingWidget, unit pricing calculation, unitId in localStorage for booking flow, booking page sends unitId | Done |
| I78 | 2026-04-10 | Property→Unit system — Unit info on booking detail pages | AMS | Added `BookingUnit` type. Guest booking detail shows unit name/image in property card, bedrooms from unit capacity. Guest bookings list shows unit name subtitle. Host bookings table has "Unit" column. All bilingual, backward-compatible | Done |
| I79 | 2026-04-10 | `/host/calendar` — Full host calendar page replacing placeholder | AMS | Complete calendar with property/unit selectors, 7-column grid (Sat-first), color-coded days (booked/pending/blocked/available), date range selection for blocking/unblocking, monthly bookings list, bilingual. Updated `hostApi.getCalendar` to accept query params and `blockDates` to accept optional `unitId` | Done |
| I80 | 2026-04-10 | `/listings` — Unit-aware property search with unit count display | AMS | Search no longer excludes properties when only some units are booked; "recovery" step checks per-unit availability (bookings + blocked dates). Response includes `activeUnitCount`; PropertyCard shows unit badge with Layers icon when > 0. `Property` type extended with `activeUnitCount` | Done |
| I81 | 2026-04-10 | `/admin/properties` — Unit management in admin dashboard | AMS | Backend: `getPropertyUnits` + `toggleUnitStatus` in adminController; admin routes for listing units per property and toggling unit active status. Frontend: `adminApi.getPropertyUnits` + `toggleUnit`; expandable unit sub-table in admin properties page with View/Hide toggle, unit name/status/capacity columns, and Activate/Deactivate buttons | Done |
| I82 | 2026-04-10 | Property→Unit system — Property model update with publish guard | AMS | Property model: added `units` virtual (count of active units via Unit ref). `togglePropertyStatus`: blocks activation when property has units but none are active. `deleteUnit`: auto-deactivates property when last active unit is removed. `getProperty`: now returns `activeUnitCount` in response. Backward-compatible — standalone properties (no units) unaffected | Done |
| I83 | 2026-04-10 | Bilingual property titles — `titleAr` field across full stack | AMS | Backend: added `titleAr` to Property schema (optional, max 200 chars), text index, `ALLOWED_UPDATE_FIELDS`, autocomplete search (`$or` on title/titleAr). Updated all `select`/`populate` calls across 10 controllers (booking, host, admin, message, unit, publicHost, report, bnpl, payment, property) to include `titleAr`. Frontend: added `titleAr` to Property TypeScript interface; bilingual title inputs (EN/AR side-by-side) in create + edit forms; `displayTitle` pattern (`isAr && titleAr ? titleAr : title`) applied across PropertyCard, property detail, booking detail, favorites, host profile, host listings — all references updated | Done |
| I84 | 2026-04-10 | Unit system — API integration tightening & bug fixes | AMS | Centralized `Unit` + `UnitPool` interfaces in `types/index.ts` (replacing 3 scattered inline definitions). Fixed host unit toggle: was using `unitsApi.update({isActive:true})` which fails for hosts (isActive admin-only in whitelist) — added `PATCH /units/:id/toggle` endpoint with `toggleUnitStatus` controller (mirrors property toggle pattern with cascading deactivation). Added `unitsApi.toggle()` client method. Fixed publish guard error surfacing: `handleToggle` in host listings now extracts `response.data.message` from backend errors instead of showing generic toast. Fixed UnitsList silent error: added `loadError` state instead of empty catch | Done |
| I85 | 2026-04-10 | Property→Unit — Demote property to parent container | AMS | **Backend:** Property schema — removed `required` from `description`, `pricing.perNight`, `capacity.maxGuests`; removed `default` from `capacity.bedrooms/bathrooms/beds` (these are now unit-level). Guarded `discountedPrice` virtual against undefined pricing. `getProperties` filters out properties with no active units from public search. `getMyProperties` aggregates `unitCount` + `activeUnitCount` per property. **Frontend:** Stripped create (`/host/listings/new`) and edit (`/host/listings/[id]/edit`) forms to only: name EN/AR, type, city, district, address, map picker. Removed all pricing/capacity/amenities/rules/photos sections. Host listings page shows horizontal cards with unit count badges, "No units — add units to activate" warning. After create, redirects to units page | Done |
| I86 | 2026-04-10 | Host UX — Listings, create form, unit form overhaul | AMS | **Host listings:** Properties with 0 units now show "Inactive" badge (effectiveActive logic); toggle button disabled with tooltip when no units. **Create/Edit property:** Fixed Arabic text rendering (unicode escapes → actual Arabic); added direction field (8 compass options). **Unit form complete redesign:** Removed pricing section (dedicated page later). Main amenities as big icon toggle bubbles (Bedrooms, Bathroom, Pool, Kitchen, Living Room) — each reveals dedicated detail card when toggled. Number fields → stepper UI with +/- buttons. "غرفة خارجية" → "ملحق" (Annex). Bathroom/kitchen amenities with icons. Additional Amenities with icons. Features with big icon tiles. Cancellation policy → radio cards with full Arabic/English descriptions (Free/Flexible/Moderate/Strict). Photo limit raised to 30. Written rules standalone section | Done |
| I87 | 2026-04-10 | Unit form — UX refinements: images at end, mandatory bathrooms, suitability toggle, pool UI | AMS | **UnitForm.tsx rewrite:** Photos section moved to end of form (section 9). Bathrooms is mandatory standalone section (Lock icon, "Required" badge, min=1, always visible). Main amenity toggle grid reduced to 4 items (Bedrooms, Pool, Kitchen, Majlis — removed Bathrooms). Detail cards for toggled amenities placed OUTSIDE the main toggle section as separate top-level cards. Suitability uses two independent toggle buttons (Families + Singles) instead of dropdown with "Both" option. NumberStepper uses `<input type="number">` for keyboard entry (hidden browser spinners via CSS). Arabic fixes: سعة الطعام → سعة السفرة, غرف المعيشة → مجلس. Pool settings UI: structured header bars with pool number, organized sections, fixed/variable depth toggle buttons | Done |
| I88 | 2026-04-10 | Units list — Duplicate confirmation dialog with exclusion options | AMS | Clicking Duplicate now opens a modal dialog instead of immediately duplicating. Dialog shows 3 checkboxes for what to exclude from the copy: Photos, Pricing, Blocked dates. Each checkbox is a styled card with icon (ImageOff, DollarSign, CalendarOff). Cancel button + Confirm button with loading spinner. Backend already supported `exclude` array param; frontend now passes selected exclusions via `unitsApi.duplicate(id, exclude)`. Bilingual (EN/AR) | Done |
| I89 | 2026-04-10 | Backend — Public `searchUnits` endpoint (`GET /api/v1/units/search`) | AMS | New aggregation-based search for guest browsing. Two-phase filter: property-level (city, type, district, direction, text search) then unit-level (capacity, bedrooms, amenities, pool, area, suitability, rating, discount). Date availability checks both `unavailableDates` and active bookings. Price filtering via computed `avgPrice` (average of 7-day pricing). Sort by price/rating/newest. Populates property (title, location, type, direction) and host (name, avatar, isVerified) via `$lookup`. Strips private location fields (coordinates, address). Paginated response | Done |
| I90 | 2026-04-10 | Per-date pricing override system for units | AMS | Unit schema: added `datePricing` array field (date, price, isBlocked) with compound index. New `PUT /api/v1/units/:id/pricing` endpoint with `updateUnitPricing` controller — supports weekday/weekend bulk apply, direct pricing update, and date override upsert/remove via merge map. `calculateUnitPricing` in bookingController now checks datePricing overrides first (blocked = error, price override = use it, else day-of-week fallback). Frontend: `unitsApi.updatePricing` added. `datePricing` added to ALLOWED_UNIT_FIELDS | Done |
| F156 | 2026-04-10 | Home — "Something went wrong" error when new property exists with no units | AMS | **Backend:** `getHomeFeed` now filters by `Unit.distinct('property', {isActive:true})` before querying featured/luxury/family/business/topRated — same pattern as `getProperties`. **Frontend:** `PropertyCard` now has null guards for `pricing` (uses `property.pricing?.perNight ?? 0`), `capacity` (conditionally renders stats), `images` (guards `property.images?.length`), `ratings` (optional chaining). Prevents TypeError when property has no pricing/capacity data | Done |
| F157 | 2026-04-10 | Units — "Failed to load units" when property is toggled inactive | AMS | Backend `getProperty` was blocking all inactive properties regardless of caller. Now checks `isHost` and `isAdmin` before returning 404 — owners and admins can view their inactive properties. The units management endpoint (`getMyPropertyUnits`) was already unrestricted | Done |
| F158 | 2026-04-10 | UnitForm — سعة السفرة missing "guests" context | AMS | Changed label from "Dining Seats" / "سعة السفرة" to "Dining Capacity (guests)" / "سعة السفرة (ضيوف)" | Done |
| I91 | 2026-04-10 | Host listings — Property image fallback to unit images | AMS | Backend `getMyProperties` now aggregates first unit image per property via `Unit.aggregate` (matches units with images, groups by property, takes first image). Returns `unitImage` field on properties that have no images of their own. Frontend host listings page uses `property.images?.[0]?.url \|\| property.unitImage?.url` for thumbnail display | Done |
| I92 | 2026-04-10 | Route rename — `/listings` → `/search` sitewide | AMS | Renamed `frontend/src/app/listings/` directory to `frontend/src/app/search/`. Added 308 permanent redirect in `middleware.ts` for `/listings` and `/listings/*`. Updated ~45 route references across 19 files (Header, Footer, HeroSearch, FeaturedListings, CategoryNav, CityBrowse, SearchFilters, PropertyCard, booking pages, dashboard pages, host profile). Matcher config extended. `/host/listings` routes untouched | Done |
| I93 | 2026-04-10 | Unit-based search — search page shows units instead of properties | AMS | **Backend:** New `GET /api/v1/units/search` aggregation endpoint with property-level + unit-level filters, date availability, price averaging, sort, pagination, and $lookup for property/host. **Frontend:** New `UnitCard` component (`components/listings/UnitCard.tsx`) with image carousel, unit name, property subtitle, capacity, pricing, ratings. Search page (`/search`) now calls `unitsApi.search()` and renders `UnitCard` instead of `PropertyCard`. API client: added `unitsApi.search()` | Done |
| I94 | 2026-04-10 | Unit detail page — `/search/[id]` shows unit instead of property | AMS | Rewrote `/search/[id]` to fetch via `unitsApi.getOne(id)`. Shows unit images, unit name/description, unit capacity (guests/bedrooms/bathrooms), unit amenities/features, unit cancellation policy + written rules. Property data (location, map, type, host) comes from populated `unit.property`. Dual-ID resolution: if unit fetch fails, tries property ID → fetches first active unit. Reviews still from property level. BookingWidget still receives property (already unit-aware) | Done |
| I95 | 2026-04-10 | Unit pricing calendar page for hosts | AMS | New page at `/host/listings/[id]/units/[unitId]/pricing`. Weekday (Sun-Wed) and weekend (Thu-Sat) price inputs with "Apply" buttons. Monthly calendar grid (Saturday-first, Saudi standard) showing per-day prices with color-coded indicators: blocked (red), custom price (blue), weekend (amber), today (ring). Click any future day to set special price, block/unblock, or remove override. Day-of-week price badges. Pricing link added to units list page. `Unit` type updated with `datePricing` field | Done |
| I96 | 2026-04-10 | Sitewide — Units displayed instead of properties | AMS | **FeaturedListings:** switched from `propertiesApi.getAll` + `PropertyCard` to `unitsApi.search` + `UnitCard`. **Host profile:** Properties tab renamed to Units, fetches units via `unitsApi.getForProperty` for each property, renders `UnitCard`. **Booking flow:** already unit-aware (BookingWidget, booking creation, booking list, booking detail). **Favorites:** links to `/search/{propertyId}` resolved via dual-ID fallback in detail page. Home feed filtered to only properties with active units | Done |
| F159 | 2026-04-11 | `/search/[id]` — "Something went wrong" crash on unit detail page | AMS | `BookingWidget` accessed `property.pricing.perNight` but `getUnit` populate didn't include `pricing` or `capacity` fields. **Backend:** added `pricing capacity` to `getUnit` property populate select. **Frontend:** added `?.` / `?? 0` null guards on all `property.pricing`, `property.capacity`, and `property.ratings` accesses in BookingWidget (13 locations). Also added `initialUnitId` prop so unit detail page auto-selects the current unit in the booking widget dropdown | Done |
| I97 | 2026-04-11 | `/search` — Wishlist heart button on UnitCard (same as PropertyCard) | AMS | Copied full wishlist picker from PropertyCard to UnitCard: heart icon at bottom-right of image, list picker portal with multi-list checkboxes, create-new-list, clear-all. Uses `property._id` (not unit ID) since wishlists are property-based. Added `px-2 py-0.5` to type badge for padding. Imports: `useAuth`, `wishlistsApi`, `createPortal`, `toast`, `WishlistList` type | Done |
| I98 | 2026-04-11 | `/host/listings/[id]/units/[unitId]/pricing` — Rename "Block/حظر" to "Reserved/محجوز" | AMS | Changed 6 translation keys: block→Reserved/محجوز, unblock→Unreserve/إلغاء الحجز, blocked→Reserved/محجوز, priceBlocked→Date reserved/تم حجز التاريخ, priceUnblocked→Date unreserved/تم إلغاء حجز التاريخ, blockedDay→Reserved/محجوز | Done |
| I99 | 2026-04-11 | `/host/listings/[id]/units/[unitId]/pricing` — Enter key submits pricing values | AMS | Added `onKeyDown` Enter handlers to weekday input (triggers `applyWeekday`), weekend input (triggers `applyWeekend`), and special price input (triggers `saveSpecialPrice`) | Done |
| I100 | 2026-04-11 | `/host/listings` — Inline expandable units on property cards | AMS | Replaced "Units" navigation link with expand/collapse toggle (ChevronDown/Up). Clicking fetches units via `unitsApi.getManage` and shows them inline below the property card with thumbnail, bilingual name, capacity stats, avg price, active/inactive badge, Edit/Pricing links, and toggle button. Footer has "Add Unit" + "Manage all units" link. Units cached in state after first load | Done |
| I100 | 2026-04-11 | `/host/listings/[id]/units/[unitId]/pricing` — Individual day-of-week pricing | AMS | New section below weekday/weekend quick-apply with 7 individual day inputs in Saudi week order (Sat-Fri). Each day has labeled input pre-populated from `unit.pricing`, SAR symbol, Apply button, and Enter key support. Responsive grid (2 cols mobile, 3-4 desktop). Weekend days highlighted in amber. New `dayInputs` state, `applyDayPrice` function calling `unitsApi.updatePricing`. New translations: `individualDays`, `individualDaysDesc` | Done |
| F160 | 2026-04-11 | BookingWidget — Price calculation ignores `datePricing` overrides | AMS | BookingWidget only used `selectedUnit.pricing[dayName]` for per-day prices, ignoring host-set date-specific price overrides and blocked dates from `unit.datePricing[]`. Added `datePricing` to `UnitOption` interface. Pricing loop now builds a date override map and checks per-date overrides first (matching backend `calculateUnitPricing` logic). Blocked dates zero out pricing and show a red warning banner (AR/EN) above the Book button | Done |
| I101 | 2026-04-11 | `/host/listings/[id]/units/[unitId]/pricing` — Calendar first day of week changed from Saturday to Sunday | AMS | `DAY_HEADERS` reordered Sun-Sat. `getDaysInMonth` offset changed from `(getDay()+1)%7` (Saturday-first) to `getDay()` (Sunday-first). `dayBadges` order changed from `[6,0,1,2,3,4,5]` to `[0,1,2,3,4,5,6]`. Individual day inputs reordered Sunday-first. All short name arrays updated to match | Done |
| I102 | 2026-04-11 | `/host/listings/[id]/units/[unitId]/pricing` — Scrollability and compact layout | AMS | Added `py-6 pb-20` to main wrapper for proper vertical spacing and bottom scroll room. Individual day prices section redesigned from 4-column card grid (with full labels and separate Apply buttons) to compact 7-column row on desktop / 4-column on mobile. Each cell: day abbreviation, centered number input (no spinner), small Apply button. Saves significant vertical space | Done |
| F161 | 2026-04-11 | `/host/listings/[id]/units` — Units don't load when property is inactive | AMS | `load()` used `Promise.all` for units + property fetch; if `getOne` returned 404 (inactive property), entire Promise.all rejected and units never loaded. Split into separate try/catch blocks so property fetch failure doesn't block unit loading | Done |
| I103 | 2026-04-11 | `/host/calendar` — Backend endpoint for unit booked dates | AMS | New `getUnitBookedDates` controller (`GET /api/v1/bookings/unit/:unitId/dates`) returns active bookings (pending, confirmed, non-expired holds) with checkIn/checkOut/status/guest. Route added before `/:id` to avoid param conflicts. Frontend `bookingsApi.getUnitBookedDates(unitId)` added | Done |
| I104 | 2026-04-11 | `/admin/properties` — Admin delete buttons for properties and units | AMS | Added `deleteProperty` and `deleteUnit` methods to `adminApi` in `api.ts`. Admin properties page: `deleteProperty` handler with confirm dialog, success/error toasts, reload; `deleteUnit` handler with confirm, reload units for expanded property. Dark gray "Delete" button per property row (with Trash2 icon, loading spinner) and "Delete" button per unit row | Done |
| I105 | 2026-04-11 | `/search` — UnitCard date-aware pricing | AMS | UnitCard now uses `checkIn`/`checkOut` props to compute average nightly price based on selected dates' day-of-week rates. Shows total price and "/ N nights" label when dates selected. New `avgPriceForDates()` helper | Done |
| I106 | 2026-04-11 | ImageGallery — Individual image click + video button + i18n | AMS | Each grid image individually clickable, opens lightbox at that image index. Added optional `videoCount`/`onVideoClick` props. "Show all photos" text now bilingual. Uses `useLanguage()` | Done |
| I107 | 2026-04-11 | `/search/[id]` — Scroll-to-section navigation | AMS | Converted from tab-switching to scroll-to-section (all sections always visible). IntersectionObserver highlights active nav pill. Each section has `ref` + `id` + `scroll-mt-20` | Reverted |
| I108 | 2026-04-11 | `/search/[id]` — Host info above segmented nav | AMS | Extracted host info card from Specification tab to always-visible position between gallery and nav pills | Reverted |
| I109 | 2026-04-11 | `/search/[id]` — Wishlist picker on heart click | AMS | Ported full wishlist list-picker from UnitCard. Heart opens portal with all lists + checkboxes, create new list, remove all | Done |
| I110 | 2026-04-11 | UnitForm — Sticky section navigation | AMS | Sticky horizontal pill-bar nav at top of form with 8 section buttons. Click scrolls to section. Added `id` attributes to all section containers | Done |
| I111 | 2026-04-11 | UnitForm — Security deposit labels | AMS | Renamed "Insurance" → "Security Deposit", "Insurance on Arrival" → "Require security deposit (refundable after checkout)". Arabic updated | Done |
| F162 | 2026-04-11 | `/search/[id]` — Revert to tab-switching, fix host info position | AMS | Reverted I107+I108 per user feedback. Host info moved back inside left column (after quick stats, always visible). Segmented nav placed directly under host info. Changed from scroll-to-section (all sections visible + IntersectionObserver) to tab-switching (conditional rendering, one tab at a time). Removed `SECTION_IDS`, `sectionRefs`, IntersectionObserver, `scrollToSection`. Nav buttons now call `setActiveSection()` | Done |
| I112 | 2026-04-11 | `/search` — UnitCard shows weekly + monthly average pricing when no dates selected | AMS | When `!checkIn \|\| !checkOut`, UnitCard now shows 3 price tiers: per night (SAR X / ليلة), per week (SAR X / أسبوع), per month (SAR X / شهر). Weekly price applies `unit.pricing.weeklyDiscount` if set. Nightly line uses `text-sm font-semibold`, weekly/monthly use `text-sm font-bold`. Existing date-range display preserved when dates are selected. All prices use `formatPriceNumber` + `SarSymbol`, bilingual labels via `isAr` | Done |
| I113 | 2026-04-11 | BookingWidget — Host-blocked dates shown as unavailable in calendar | AMS | MiniCalendar `unavailableDates` now includes host-blocked dates from `selectedUnit.datePricing` (filtered by `isBlocked`) and unit-specific booked dates fetched via `bookingsApi.getUnitBookedDates(selectedUnitId)`. New `unitBookedDates` state, new `useEffect` that fetches booked date ranges when `selectedUnitId` changes and expands them into individual date strings | Done |
| I114 | 2026-04-11 | BookingWidget — Hide unit selector when viewing a specific unit | AMS | When `initialUnitId` is provided (user navigated to `/search/[unitId]`), the unit selector dropdown is hidden. Changed condition from `units.length > 0` to `units.length > 0 && !initialUnitId` | Done |
| I115 | 2026-04-11 | `/host/listings/[id]/units` and `/host/listings` — Unit action buttons styled as chip buttons | AMS | Replaced plain text links (Edit, Calendar, Duplicate) with styled chip buttons: `inline-flex items-center gap-1.5 px-3 py-1.5 bg-gray-100 hover:bg-gray-200 rounded-lg text-xs font-medium`. Each chip has an icon (Pencil, Calendar, Copy) + bilingual label. Container changed from `gap-3` to `gap-2`. Toggle button kept separate. Applied consistently in both units page and listings expandable unit rows | Done |
| F163 | 2026-04-11 | `/dashboard/favorites/[listId]` — Page crashes after Property→Unit migration | AMS | Added `?.` optional chaining + `?? 0` fallbacks to all `property.capacity`, `property.pricing`, `property.ratings` accesses in `getMismatchReasons` function and `PropertyListCard` JSX. Fields: `capacity.maxGuests`, `capacity.bedrooms`, `capacity.bathrooms`, `pricing.perNight`, `pricing.discountPercent`, `ratings.average`, `ratings.count` | Done |
| F164 | 2026-04-11 | `/booking/[id]` — Page crashes when id is a unit ID (404) and when pricing fields undefined | AMS | `fetchProperty` now tries `unitsApi.getOne(id)` first (extracts `unit.property`), falls back to `propertiesApi.getOne(id)`. Added `?.` + `?? 0` to `property.pricing.perNight`, `property.pricing.cleaningFee`, `property.pricing.discountPercent`, `property.capacity.maxGuests` | Done |
| F165 | 2026-04-11 | Sitewide — "Set as main" image not displayed first after saving | AMS | `isPrimary` was correctly persisted to the database, but ImageGallery, UnitCard, PropertyCard, and search detail page all displayed images in array order, ignoring `isPrimary`. Added `.sort((a,b) => (b.isPrimary?1:0) - (a.isPrimary?1:0))` to ensure the primary image always appears first in galleries, carousels, and cards. Fixed in ImageGallery.tsx (grid + lightbox), UnitCard.tsx, PropertyCard.tsx, and search/[id]/page.tsx | Done |
| F166 | 2026-04-11 | `/host/listings` — Property stays active in UI after last unit deactivated | AMS | Backend auto-deactivates property when last unit is toggled off, but frontend local state wasn't updated. `handleUnitToggle` now computes updated units array, checks if all are inactive, and if so sets `property.isActive = false` and `activeUnitCount = 0` in local state. Also syncs `activeUnitCount` on every unit toggle | Done |
| I116 | 2026-04-11 | `/host/listings` — Amber warning banners when property toggle is disabled | AMS | When property has units but all are inactive, shows amber banner: "Activate at least one unit to enable this property" / "فعّل وحدة واحدة على الأقل لتفعيل العقار". When property has no units, upgraded plain text to matching amber banner: "Add at least one unit to activate this property" / "أضف وحدة واحدة على الأقل لتفعيل العقار". Both use `bg-amber-50 border border-amber-200 text-amber-700 rounded-lg p-2 text-xs flex items-center gap-1.5` with AlertTriangle icon | Done |
| I117 | 2026-04-11 | `/host/pricing` — New pricing management page + sidebar link | AMS | Added "Pricing" / "الأسعار" link to HostSidebar `getNavItems` (after Calendar, DollarSign icon). New `/host/pricing/page.tsx`: lists all host properties as collapsible cards. Click expands to show units with weekday/weekend average prices (computed from `unit.pricing`). "Set Pricing" button per unit links to `/host/listings/{propertyId}/units/{unitId}/calendar`. Lazy-loads units via `unitsApi.getManage`, caches in state. Bilingual translations, loading states, empty states | Done |
| I118 | 2026-04-11 | UnitForm — Vertical sidebar navigation on desktop | AMS | Converted sticky horizontal pill bar to vertical sidebar nav on `lg:` screens. Flex layout: `nav` (sticky top-24, w-48, hidden below lg) + form body (flex-1). IntersectionObserver tracks visible section and highlights active nav item (`bg-primary-50 text-primary-700 font-semibold border-l-2 border-primary-500`). Mobile keeps horizontal sticky bar as fallback (`lg:hidden`). Both navs highlight active section | Done |
| I119 | 2026-04-11 | `/property/[id]` — New property detail page showing all listed units | AMS | New page at `frontend/src/app/property/[id]/page.tsx`. Fetches property via `propertiesApi.getOne` and units via `unitsApi.getForProperty`. Displays Header, breadcrumb (Home > Search > City > Property Name), property title (bilingual), type badge, location, ratings, ImageGallery, "Available Units" section with 2-column UnitCard grid (date-aware pricing from cookies), empty state, Footer. Loading skeleton and not-found states. Also made `propertyTitle` in `/search/[id]` a clickable Link to `/property/{id}` | Done |
| I120 | 2026-04-11 | Unit calendar — Pricing/Discount tab toggle | AMS | Added segmented tab bar (Pricing / Discount) after page header. Pricing tab shows existing day-of-week pricing settings. Discount tab shows three sections: Global Discount (% input saved via `pricing.discountPercent`), Weekly Discount (% input for 7+ night stays via `pricing.weeklyDiscount`), and Date-Specific Discounts (instructions to use calendar multiselect). Active discount badges shown inline. Tab state: `activeTab` with `'pricing' | 'discount'` | Done |
| I121 | 2026-04-11 | Unit calendar — Multiselect date range | AMS | Ported two-click range selection from host/calendar page. First click sets start date, second click completes range (auto-sorts start/end). Same-day double-click opens single-day popover. Selected range highlighted in blue-100 with ring-1 ring-blue-300. Selection Action Bar shows formatted range label ("Apr 15 - Apr 20 (6 days)"), Reserve/Unreserve buttons, Set Price (pricing tab) or Set Discount (discount tab) inline inputs. Actions apply to all dates via `unitsApi.updatePricing({ datePricing: [...] })`. Month navigation and Clear button reset selection | Done |
| I122 | 2026-04-11 | Unit calendar — Discount management controls | AMS | Under Discount tab: Global Discount section with % input + save button (persists to `unit.pricing.discountPercent`), Weekly Discount section for 7+ night stays (persists to `unit.pricing.weeklyDiscount`), Day-Specific Discounts section with instructions to use multiselect. When date range selected in discount tab, "Set Discount" button appears in action bar with % input — computes discounted prices per day (base price * (1 - discount/100)) and saves as datePricing overrides. Green badges show active discount percentages | Done |
| I123 | 2026-04-11 | `/host/calendar` — Redesigned as multi-property overview with mini calendars | AMS | Replaced single-calendar-with-dropdown design with all-properties overview. Fetches all properties via `propertiesApi.getMyProperties()`, units via `unitsApi.getManage()`, bookings via `bookingsApi.getUnitBookedDates()`. Each property section shows its units as a responsive grid (1 col mobile, 2 md, 3 lg) of mini calendars. Each mini calendar: unit name label, 7-column Sun-Sat grid, color-coded days (green=confirmed, yellow=pending, red=host-reserved from `datePricing.isBlocked`, white=available). Clicking a unit navigates to `/host/listings/{propertyId}/units/{unitId}/calendar`. Shared month navigation, legend at top, loading skeletons, bilingual labels, data cached to avoid refetch on month change | Done |
| I124 | 2026-04-11 | Unit model — `discountRules` array and `datePricing.discountPercent` field | AMS | Added `discountRules` top-level array to Unit schema with `type` (enum: weekday/weekend) and `percent` (0-100). Added `discountPercent` (Number, 0-100) to `datePricing` subdocument for per-date discount overrides. `discountRules` added to `ALLOWED_UNIT_FIELDS`. `updateUnitPricing` preserves `discountPercent` during datePricing merge and accepts `req.body.discountRules` to set discount rules. Frontend `Unit` type updated with both fields | Done |
| I125 | 2026-04-11 | `/search` — Filter out units with blocked datePricing dates in search | AMS | `searchUnits` now excludes units that have `datePricing` entries with `isBlocked: true` overlapping the requested check-in/check-out range via `$not: { $elemMatch }` filter. Works alongside existing `unavailableDates` and booking overlap filters | Done |
| I126 | 2026-04-11 | UnitCard — "Price not set" fallback when basePrice is 0 | AMS | When `basePrice === 0`, UnitCard previously rendered nothing in the pricing area. Now shows bilingual fallback: "السعر غير محدد" / "Price not set" in `text-sm text-gray-400` | Done |
| I127 | 2026-04-11 | HostSidebar — Pricing icon changed from DollarSign to Tag | AMS | Pricing and Earnings both used `DollarSign` icon, making them visually identical. Changed Pricing to `Tag` (from lucide-react) to differentiate. Earnings keeps `DollarSign` | Done |
| I128 | 2026-04-11 | `/host/bookings` — Bilingual property title + price fallback | AMS | Property title column now uses `lang === 'ar' ? (titleAr \|\| title) : title` pattern for bilingual display. Added `titleAr` to Booking interface's property type. Amount column now shows gray dash fallback when `totalPrice` is null/undefined instead of rendering empty | Done |
| F167 | 2026-04-11 | `/property/[id]` — Broken images after Property-to-Unit migration | AMS | `galleryImages` only used `property.images`, but after migration images live at unit level. Added fallback: if `property.images` is empty, aggregates images from all units via `units.flatMap(u => u.images)`, sorted by `isPrimary`. Falls back to placeholder only if both property and unit images are empty | Done |
| F168 | 2026-04-11 | BookingWidget — Shows 0 price when selected dates include blocked dates | AMS | Pricing loop broke out on blocked dates and zeroed `subtotal`/`pricePerNight`. Removed `break` and zeroing logic. Blocked dates now use day-of-week default price for price computation so the full estimated price is always shown. Blocked-date warning banner still displays | Done |
| I129 | 2026-04-11 | Unit calendar — Page title changed from "Pricing & Availability" to "Calendar" | AMS | Updated `t.title` translations to `{ en: 'Calendar', ar: 'التقويم' }` and `usePageTitle` to match | Done |
| F169 | 2026-04-11 | Unit calendar — "Unreserve" Arabic translation incorrect | AMS | Changed `unblock` Arabic from `'إلغاء الحجز'` to `'إلغاء التخصيص'` to distinguish from booking cancellation | Done |
| I130 | 2026-04-11 | Unit calendar — Distinguish booked events by status (completed/upcoming/pending) | AMS | Added `checkOut` to `BookedDateInfo` interface and stored it in bookedDates map. Cell backgrounds now status-aware: pending=`bg-yellow-50`, completed (confirmed + checkOut past)=`bg-gray-100`, upcoming (confirmed + checkOut future)=`bg-emerald-50`. Labels and icon colors match: pending=yellow "Pending"/"قيد الانتظار", completed=gray "Completed"/"مكتمل", upcoming=emerald (existing). Legend updated with 3 entries: Upcoming/قادم (green), Pending/قيد الانتظار (yellow), Completed/مكتمل (gray) | Done |
| F170 | 2026-04-11 | `/booking/[id]` — Page shows no values, "Guests Up to 0 allowed", zero pricing | AMS | Booking page failed when `id` is a unit ID: (1) if `unit.property` was a string, stored the unit object as property — now fetches the full property separately; (2) pricing used `property.pricing.perNight` which doesn't exist on units — now computes from unit day-of-week pricing (`sunday`..`saturday`); (3) guest capacity showed 0 — now reads `unit.capacity.maxGuests` with property fallback; (4) images used non-optional `property.images.find()` — now uses optional chaining with unit image fallback; (5) discount label sourced from unit pricing when available | Done |
| I131 | 2026-04-11 | `/dashboard/favorites/[listId]` — Show units instead of properties | AMS | Replaced PropertyListCard rendering with UnitCard grid. For each wishlisted property, fetches units via `unitsApi.getForProperty`. Displays property title header (bilingual) with remove button, then 2-column UnitCard grid below. Shows "No units available" if property has no units. Spinner while units load. Mismatched section also uses UnitCard. Date-aware pricing passed via `checkIn`/`checkOut` props | Done |
| I132 | 2026-04-11 | Unit calendar — Replace inline "Set Price" input with modal dialog | AMS | Removed `showRangePriceInput` toggle state and inline input/button block from multiselect action bar. Replaced with a button that opens `showRangePriceDialog` modal. Dialog has price input with SAR symbol, Enter-key support, Cancel/Save buttons. Consistent with other pricing dialogs on the page | Done |
| I133 | 2026-04-11 | Unit calendar — Discount Rules UI overhaul | AMS | Replaced empty "Day-Specific Discounts" section with full "Discount Rules" feature. New state: `discountRules`, `showAddDiscountDialog`, `newDiscountType`, `newDiscountPercent`, `showRangeDiscountDialog`, `rangeDiscountPercent`. Discount Rules section shows weekday/weekend rules with percent badges and delete buttons. "Add Discount" dialog with day-type selector (disables already-existing types) and slider+input for percent. Multiselect "Set Discount" replaced with dialog (slider 0-100% + input). `applyRangeDiscount` saves `discountPercent` per date via `unitsApi.updatePricing`. `handleAddDiscountRule`/`handleRemoveDiscountRule` persist to `unit.discountRules` via `unitsApi.update`. Calendar cells show orange discount badge (`-X%`) for effective discount (max of rule-based and date-specific). Saudi weekends (Thu-Sat) used for rule matching. `DatePricingEntry` interface extended with `discountPercent`. Imports: added `Trash2`, `Plus` | Done |
| I134 | 2026-04-11 | `/host/calendar` — 8-entry legend and status-aware mini calendar cells | AMS | Expanded `getUnitDayStatus` return type to include `'completed'` and `'override'`. datePricing entries with `!isBlocked && price > 0` map to `'override'`; confirmed bookings with past checkOut map to `'completed'`. Legend upgraded from 4 circles to 8 rounded squares: Default (white), Custom price (blue-50), Reserved (red-50), Upcoming (emerald-50), Pending (yellow-50), Completed (gray-100), Weekend (amber-50), Today (ring-2 ring-primary-400). MiniCalendarGrid cells now show emerald-100 for confirmed, gray-100 for completed, blue-100 for override, amber-50 for Saudi weekends (Thu-Sat), and ring-2 ring-primary-400 for today. Matches unit calendar page design | Done |
| I135 | 2026-04-11 | UnitCard — Show today's 1-night price instead of 7-day average; /week and /month labels for long stays | AMS | Replaced `avgPrice()` with `todayPrice()` that reads current day-of-week rate. No-dates view now shows only the nightly rate (removed /week and /month tiers). When dates are selected for 7+ nights shows `/week` price (with weeklyDiscount), 30+ nights shows `/month` price (with monthlyDiscount) as an extra line below the total | Done |
| I136 | 2026-04-12 | Unit calendar — Single-click opens day popover with range mode toggle | AMS | Rewrote `handleDayClick`: default (normal) mode opens day popover on single click. New "Select Range" / "تحديد نطاق" toggle button near month navigation switches to range mode (two-click start/end selection). Clicking same date in range mode cancels range and opens popover. New state: `rangeMode`. Translations: `selectRange`, `normalMode` | Done |
| I137 | 2026-04-12 | Unit calendar — Set Price / Set Discount always visible in action bar | AMS | Removed `activeTab === 'pricing'` gate from Set Price button and `activeTab === 'discount'` gate from Set Discount button. Both buttons now render unconditionally inside the `selectionEnd &&` block regardless of which tab is active | Done |
| I138 | 2026-04-12 | Unit calendar — Discount section rework with long-stay discounts | AMS | Reworked discount tab layout: new "Long-stay Discounts" card with weekly (7+ nights) and monthly (30+ nights) inputs side-by-side in a 2-col grid. Discount Rules (weekday/weekend) card kept. Global Discount card moved to bottom. New state: `monthlyDiscountInput`. New function: `saveMonthlyDiscount`. Translations: `monthlyDiscount`, `monthlyDiscountDesc`, `longStayDiscounts` | Done |
| I139 | 2026-04-12 | Unit calendar — Unreserve keeps popover open with price note | AMS | `toggleBlock(false)` no longer closes the day popover (`setSelectedDate(null)` removed from unblock path). User sees updated state after unreserving and can optionally remove price override. Added "Custom price will be kept" / "سيتم الاحتفاظ بالسعر المخصص" note below Unreserve button. Translation: `keepPriceNote` | Done |
| I140 | 2026-04-12 | Unit model — `monthlyDiscount` field added to pricing subdocument | AMS | Added `monthlyDiscount: { type: Number, default: 0, min: 0, max: 100 }` to `pricing` subdocument in `backend/src/models/Unit.js`. No controller changes needed — `pricing` already in `ALLOWED_UNIT_FIELDS` and merge pattern handles new sub-fields | Done |
| F171 | 2026-04-12 | BookingWidget — 0/night when only checkIn entered + monthlyDiscount support | AMS | Added `previewPricePerNight` computation: when `checkIn && !checkOut`, computes 1-night price from checkIn date's day-of-week rate (with datePricing override check). Price header shows preview instead of 0 with hint "1-night rate (select checkout)". Added `nights >= 30` branch for `monthlyDiscount` (takes priority over weeklyDiscount if larger) | Done |
| F172 | 2026-04-12 | `/dashboard/favorites/[listId]` — "Max 0 guests" blocking fixed | AMS | Added `units` parameter to `getMismatchReasons`. Guests check now computes `maxCapacity` from `Math.max(...units.map(u => u.capacity?.maxGuests ?? 0))` instead of `property.capacity?.maxGuests ?? 0`. Call site passes `unitsByProperty[p._id] || []`. Added unit count badge next to property title when `propUnits.length > 1` | Done |
| I141 | 2026-04-12 | `/search/[id]` — Features styled with icons and localized labels | AMS | Added `getAmenityLabel, getAmenityIcon` imports from `@/lib/utils`. Features section now renders `getAmenityIcon(feature)` + `getAmenityLabel(feature, language)` instead of raw feature key strings. Uses `inline-flex items-center gap-1.5` layout | Done |
| I142 | 2026-04-12 | `/hosts/[id]` — Show unit count instead of property count | AMS | Stats box changed from `stats.propertyCount` to `hostUnits.length` (already fetched). English label changed from "Properties" to "Units". Arabic label was already correct ("عدد الوحدات"). Shows "..." while `unitsLoading` is true | Done |
| I143 | 2026-04-12 | UnitCard — Layout reordered: Rating → Icons → Name → Location → Price | AMS | Rearranged card content section from Name→Location→Icons→Price+Rating to Rating→Capacity icons→Name→Location→Price. Rating now at top as Star icon + number + count (not StarRating component). Pricing is full-width (no rating beside it). Added `Star` import from lucide-react | Done |
| F173 | 2026-04-12 | `/booking/[id]` — Shows property name instead of unit name | AMS | Sidebar summary now shows unit name (`nameEn`/`nameAr`) as primary title with property name as smaller subtitle below. Fallback to property title when no unit | Done |
| F174 | 2026-04-12 | `/booking/[id]` — Zero prices when no dates or missing datePricing | AMS | Pricing loop now uses `Math.max(nights, 1)` to always compute at least today's rate. Added datePricing override lookup per date. Added weekly/monthly discount support (additive to global discount). `subtotal` only set when real dates exist. Unit images now preferred over property images for thumbnail | Done |
| F175 | 2026-04-12 | `/booking/[id]` — Hardcoded "Free cancellation" ignoring unit policy | AMS | Cancellation section now reads `unit.cancellationPolicy` (free/flexible/normal/restricted) with bilingual labels. Falls back to `unit.cancellationDescription` for subtitle text, or default message | Done |
| I144 | 2026-04-12 | UnitForm — Sidebar no longer squeezes form content | AMS | Added `lg:justify-center` to flex container and `max-w-3xl` to form body. Sidebar now hangs outside the content area instead of compressing it | Done |
| I145 | 2026-04-12 | UnitForm — Dynamic nav sections for toggled amenities | AMS | Replaced static `NAV_SECTIONS` array with `useMemo` that conditionally adds Bedrooms, Pools, Kitchen, Living Room sections when their toggles are active. IntersectionObserver dependency updated to `[NAV_SECTIONS]` to re-observe on section changes. Added `id` attributes to bedroom, pool, kitchen, living room cards | Done |
| I146 | 2026-04-12 | `/host/listings` — Units section visual distinction and button prominence | AMS | Units container now has `bg-gray-50/50 rounded-lg border border-gray-100 divide-gray-200`. Unit rows have hover state `hover:bg-white/80`. Edit button restyled to white bg with border and primary hover. Calendar button restyled with primary-50 bg and primary border/text. Both have `shadow-sm` | Done |
| F176 | 2026-04-13 | `/booking/[id]` — Page broken when navigated with property ID instead of unit ID | AMS | `fetchProperty` now handles property ID fallback: when unit fetch fails (property ID given), fetches the property then tries localStorage `hostn_unit_{id}` for the selected unit, or falls back to `unitsApi.getForProperty(id)` picking the first active unit. Previously `unit` stayed null causing 0 guests, 0 pricing, hardcoded cancellation, property name shown | Done |
| F177 | 2026-04-13 | UnitForm — Sidebar squeezing form to 544px due to page wrapper | AMS | Page wrapper on edit and new pages was `max-w-3xl` (768px). Sidebar (w-48=192px + gap-8=32px) left only 544px for form. Changed page wrappers to `max-w-5xl` (1024px), removed `lg:justify-center` from flex container. Form body now gets full 768px (`max-w-3xl` on form body). Fixed in both `[unitId]/edit/page.tsx` and `units/new/page.tsx` | Done |
| F178 | 2026-04-13 | Wishlist — Sibling UnitCards from same property don't sync heart state | AMS | After toggling wishlist, only local `isWishlisted` was updated. `user.wishlist` in AuthContext was never synced, so other UnitCards showing same property kept stale heart state. Now calls `updateUser()` after toggle/create/clear to update `user.wishlist`, triggering `useEffect` in all sibling cards. Also fixed picker list count not updating after toggle (`setLists` with updated `propertyCount`) | Done |
| F179 | 2026-04-14 | Wishlist system — Convert from property-based to unit-based | AMS | **Full migration.** Backend: `Wishlist.properties[]` (ref Property) → `Wishlist.units[]` (ref Unit). Controller methods renamed: `toggleProperty` → `toggleUnit`, `getPropertyMembership` → `getUnitMembership`, `moveProperty` → `moveUnit`. Routes updated: `/properties/:propertyId` → `/units/:unitId`. `getList` now populates units with nested `unit.property` (including host). `getLists` returns `unitCount` + cover image from first unit. `authController.toggleWishlist` updated to use `units` field. Frontend: `wishlistsApi` methods renamed to match. `WishlistList` type: `propertyCount` → `unitCount`, `properties` → `units`. UnitCard saves `unit._id` instead of `propertyId`. `/search/[id]` detail page wishlists by unit ID. Favorites overview shows unit count. Favorites detail page rewritten: flat UnitCard grid (no property grouping), filters work against unit data (capacity, bedrooms, hasPool, pricing) and nested property data (location, type, direction). Remove button per-unit with hover reveal. Mismatched section shows reasons per-unit | Done |
| F180 | 2026-04-14 | Price filter ignores discounts in `/search` and `/favorites` | AMS | Backend: search aggregation pipeline now computes `effectivePrice = avgPrice × (1 − discountPercent/100)` and filters against that instead of raw `avgPrice`. A SAR 500/night unit with 50% discount now appears when max price is set to 300. Favorites: `getMismatchReasons` price check uses `getUnitEffectivePrice()` which applies `discountPercent` to the day-of-week base price before comparing against the slider value | Done |
| F181 | 2026-04-14 | [A1] Contact form doesn't send data — fake `setTimeout` success | AMS | Created `Contact` model (`backend/src/models/Contact.js`) and public POST `/api/v1/contact` route. Frontend `contactApi.send()` replaces fake timeout. Form now persists to DB | Done |
| F182 | 2026-04-14 | [A2] Host bookings page filters out 'held' bookings | AMS | Removed `status: { $ne: 'held' }` from `getHostBookings` query. Added 'held' to filter tabs, translations, and statusColors (`bg-purple-100 text-purple-700`) | Done |
| F183 | 2026-04-14 | [A3] Host bookings amount column shows '-' | AMS | Amount column now uses `booking.pricing?.total ?? booking.totalPrice` fallback. Added `pricing` to Booking interface | Done |
| F184 | 2026-04-14 | [A4] BookingWidget price header doesn't show discount | AMS | Price header now reads `unit.pricing.discountPercent`, applies `getDiscountedPrice()` to base nightly rate, and shows original price struck through with discounted price | Done |
| F185 | 2026-04-14 | [A5] "1 properties" grammar on search detail page | AMS | Added singular/plural logic: `property` vs `properties` in English, `عقار` vs `عقارات` in Arabic | Done |
| F186 | 2026-04-14 | [A6] 'held' status unstyled on host dashboard | AMS | Added `held` to `statusLabels` and `statusColors` on `/host` dashboard page | Done |
| F187 | 2026-04-14 | [A7] "1 Bookings" grammar on earnings page | AMS | Added singular/plural: `Booking` vs `Bookings`, `حجز` vs `حجوزات` | Done |
| F188 | 2026-04-14 | [A8] Monthly earnings amounts not shown | AMS | Backend returns `earnings` field, frontend read `total`. Added `earnings: number` to interface, display uses `item.earnings ?? item.total ?? 0` | Done |
| F189 | 2026-04-14 | [A9] Average Rating shows "0.0" instead of N/A | AMS | Rating now shows `N/A` / `لا يوجد` when `averageRating` is 0, only `.toFixed(1)` when > 0 | Done |
| F190 | 2026-04-14 | [A10] Host bookings actions column empty for non-pending | AMS | Actions now show accept/decline for both `pending` and `held` bookings. Cancel button shown for `confirmed` bookings | Done |
| I147 | 2026-04-14 | [A11] Home page massive blank whitespace in hero | AMS | Reduced HeroSearch min-h from `520/600/720` to `400/440/500` (mobile/sm/md) | Done |
| F191 | 2026-04-14 | [A12] Image gallery layout gap with < 5 images | AMS | Replaced single `grid-cols-4` with 2-col outer grid + nested sub-grid for side images. Main image always fills left half, side images nest in right half (single full-height for 2 images, 2×2 sub-grid for 3-5). Eliminates empty grid cells that caused gaps in RTL | Done |
| F192 | 2026-04-14 | [A13] Browser tab shows Arabic-only title in English mode | AMS | Root layout metadata changed from Arabic-only to bilingual: `'Hostn — Vacation Rentals in Saudi Arabia \| إيجارات عطلات في السعودية'`. All page-level titles already use `usePageTitle` with `isAr` checks | Done |
| I148 | 2026-04-14 | [A17] Date format inconsistent on host pages | AMS | Host dashboard and host bookings dates now use `{ month: 'short', day: 'numeric' }` for check-in and `{ month: 'short', day: 'numeric', year: 'numeric' }` for check-out, matching dashboard pages format (`Jan 15 - Jan 20, 2026`) | Done |
| I149 | 2026-04-14 | Host Finance — 5 backend models | AMS | Created `Counter` (atomic sequence generator), `HostBankAccount` (IBAN + transfer duration), `Payout` (bank transfers with auto-numbered TR-XXXXXXXXXX), `HostInvoice` (commission invoices with INV-XXXXXXXXXX), `HostStatement` (monthly ledger with entries). All with proper indexes and pre-save hooks | Done |
| I150 | 2026-04-14 | Host Finance — Backend controller + routes | AMS | `hostFinanceController.js` with 11 endpoints: summary, payouts (list+detail), invoices (list+detail), statements (list+detail with fallback aggregation), bank account CRUD, transfer duration update. 11 routes added to `host.js`. `hostFinanceApi` added to frontend API client | Done |
| I151 | 2026-04-14 | Host Finance — Sidebar + navigation | AMS | Sidebar items changed from Earnings to Finance (`/host/finance`) in both `Sidebar.tsx` and `HostSidebar.tsx`. Finance layout with 4-tab horizontal navigation: Remittances, Invoices, Statements, Payment Method | Done |
| I152 | 2026-04-14 | Host Finance — Summary landing page | AMS | `/host/finance` shows 3 summary cards (Total Paid Out, Pending Payout, Total Commission) with SAR formatting. Bank account warning banner when no account set up. 4 quick-link cards to sub-pages | Done |
| I153 | 2026-04-14 | Host Finance — Remittances page | AMS | `/host/finance/remittances` — Gathern-style payout list with date range filters, booking number search, status pills (upcoming/processing/executed/failed). Payout cards show remittance number, dates, transfer method, masked IBAN, booking count with Arabic plurals, amount. Pagination | Done |
| I154 | 2026-04-14 | Host Finance — Invoices page | AMS | `/host/finance/invoices` — Commission invoice list with date range filter. Cards show invoice number, date, period, breakdown (gross bookings, commission rate, commission, VAT), total. Status badges. Pagination | Done |
| I155 | 2026-04-14 | Host Finance — Statements page | AMS | `/host/finance/statements` — Two-panel layout: left sidebar with year selector + monthly statement list, right main area with statement detail header (opening/closing balance, credits/debits) and ledger table (date, description, reference, credit, debit, running balance). Fallback support for aggregated data | Done |
| I156 | 2026-04-14 | Host Finance — Payment Method page | AMS | `/host/finance/payment-method` — Two cards: (1) Bank account with display/edit/add modes, IBAN validation (SA + 22 digits), masked IBAN display, delete confirmation modal. (2) Transfer duration with default/custom radio, hour input (24-336). Bilingual AR/EN | Done |
| I157 | 2026-04-14 | Host Finance — Earnings redirect | AMS | Old `/host/earnings` page converted to client redirect to `/host/finance/statements`. Backend `/api/v1/host/earnings` route kept for backward compatibility | Done |
| I158 | 2026-04-16 | Transfer Duration — 3 radio options (Gathern-style) | AMS | Payment method page updated: replaced default/custom radio with 3 Gathern-style options: direct transfer (48h), amount threshold (SAR input), weekly (day-of-week selector). Backend `updateTransferDuration` accepts `type` (direct/threshold/weekly), `thresholdAmount`, `weeklyDay`. HostBankAccount schema updated with new fields | Done |
| I159 | 2026-04-16 | Host Loyalty — Backend model + controller | AMS | `HostLoyaltySnapshot` model stores quarterly loyalty evaluations per host. `hostLoyaltyController.js` with 2 endpoints: `getLoyaltyStatus` (GET /host/loyalty?quarter=) computes 6 metrics via Booking.aggregate + Review queries, per-metric tier assignment, weakest-link overall tier, upserts snapshot; `getLoyaltySummary` (GET /host/loyalty/summary) lightweight read for dashboard widget | Done |
| I160 | 2026-04-16 | Host Loyalty — 4-tier system with 6 metrics | AMS | Tier levels: basic/silver/gold/summit. 6 quarterly metrics: confirmed nights, average reviews, unit availability, bookings rated by guests, bookings you rated, cancelled bookings (reverse — lower is better). Overall tier = weakest metric. Benefits per tier: cashback %, bonus points, certificate, badge, monthly report | Done |
| I161 | 2026-04-16 | Host Loyalty — Frontend page `/host/loyalty` | AMS | Full loyalty page with: TierBanner (gradient per tier with stars), QuarterSelector (Q1–Q4 dropdown), 6 LoyaltyMetricCards in 2-col grid with horizontal 4-dot tier track and orange value marker, MetricInfoDialog per metric (how calculated + how to improve tips), AboutProgramDialog with 3 tabs (Tiers & Rewards, Comparison table, FAQ accordion), PredictionBanner. Bilingual AR/EN | Done |
| I162 | 2026-04-16 | Host Loyalty — Dashboard widget + sidebar entries | AMS | Dashboard (`/host`) shows compact gradient tier banner with stars + "View All" link to `/host/loyalty`. Graceful degradation if API fails. Sidebar entries added to both `HostSidebar.tsx` and `Sidebar.tsx` with Award icon, label "Loyalty" / "برنامج الولاء" | Done |
| F193 | 2026-04-16 | Calendar — Monthly discount Save button blocked by shared saving flag | AMS | Changed `savingDiscount` shared state into 3 independent flags (`savingGlobalDiscount`, `savingWeeklyDiscount`, `savingMonthlyDiscount`) so saving one discount doesn't disable Save buttons on the others | Done |
| F194 | 2026-04-16 | Discount rules + monthly discount not applied in booking | AMS | Backend `calculateUnitPricing()` now applies `monthlyDiscount` (30+ nights), `weeklyDiscount` (7+ nights) with proper precedence, and per-night `discountRules` (weekday/weekend %). Frontend BookingWidget mirrors same logic. Returns `discountType` and `discountPercent` for display | Done |
| F195 | 2026-04-16 | Host listings city/district show English-only | AMS | `locationSummary()` now looks up city/district values in `CITIES`/`DISTRICTS` constants from `@/lib/constants` to display bilingual names based on current language | Done |
| I163 | 2026-04-16 | Host listings — Add New Unit button | AMS | Added "Add New Unit" button next to "Add New Property" in header. Shows dropdown of host's properties; clicking one navigates to `/host/listings/{propertyId}/units/new` | Done |
| F196 | 2026-04-16 | Booking total format changed to "Total X nights SAR Y" | AMS | BookingWidget total summary changed from `${nightLabel} = SAR ${subtotal}` to `Total ${nightLabel} SAR ${subtotal}` / Arabic `الإجمالي ${nightLabel} SAR ${subtotal}` | Done |
| I164 | 2026-04-16 | Booking discount shows type details | AMS | Price breakdown discount line now shows type (e.g., "Weekly Discount 10%", "Monthly Discount 15%", "Global Discount 5%") instead of generic "Discount" label. Bilingual AR/EN | Done |
| F197 | 2026-04-16 | Blocked dates — button enabled + breakdown shown | AMS | Added `hasBlockedDates` boolean. When selected dates include blocked days: Book button disabled, price breakdown hidden, only red warning shown | Done |
| F198 | 2026-04-16 | Global discount + check-in only → price shows 0 | AMS | `baseNightlyPrice` fallback chain now includes `previewPricePerNight` before property `perNight`, so discounted header shows correct 1-night preview when only check-in selected with unit pricing | Done |
| F199 | 2026-04-16 | Image gallery small on desktop | AMS | Gallery grid height increased from `h-80 md:h-96` to `h-80 md:h-[28rem] lg:h-[32rem]` for larger display on desktop screens | Done |
| F200 | 2026-04-16 | Gallery lightbox close button not RTL-aware | AMS | Changed `right-4` to `ltr:right-4 rtl:left-4` on lightbox close button for proper RTL positioning | Done |
| F201 | 2026-04-16 | Breadcrumb shows "Home/Properties/City" instead of property type | AMS | Breadcrumb changed from `Home > Properties > City > Unit` to `Home > {Property Type} > {Unit Name}`. Type links to `/search?type={type}` using `getPropertyTypeLabel()` for bilingual display | Done |
| F202 | 2026-04-16 | Search price filter excludes units based on weekly avg not selected dates | AMS | Backend `searchUnits` now computes `avgPrice` using only the days within the guest's selected date range (checkIn/checkOut) instead of the 7-day average. Falls back to weekly average when no dates selected | Done |
| I165 | 2026-04-17 | Subdomain migration Phase 1 — split User into Guest/Host/Admin | AMS | Created `Guest`, `Host`, `Admin` Mongoose models backed by shared `_sharedUserFields` helper (name, email, phone, password, avatar, tokenVersion, deviceTokens). Same phone can exist independently in each collection. Added `backend/scripts/split-user-to-roles.js` migration (dry-run + commit modes) that preserves `_id` across collections and backfills polymorphic discriminators on Notification/RefreshToken/Wallet/WalletTransaction/Message/Conversation/Report/SupportTicket/ActivityLog | Done |
| I166 | 2026-04-17 | Subdomain migration Phase 1 — update 14 role-specific refs | AMS | Changed `ref: 'User'` to role-specific models: Booking.guest→Guest, Property.host→Host, Review.guest→Guest, HostBankAccount/HostInvoice/HostStatement/HostLoyaltySnapshot/Payout.host→Host, BlogPost.author→Admin, Report.reviewedBy→Admin, SupportTicket.assignedTo→Admin, SavedPaymentMethod/Wishlist/Payment.user→Guest | Done |
| I167 | 2026-04-17 | Subdomain migration Phase 1 — polymorphic refs with refPath discriminators | AMS | Added `userType`/`senderType`/`reporterType`/`actorType`/`blockedByType` discriminator fields and Mongoose `refPath` to Notification, RefreshToken, Wallet, WalletTransaction, Message.sender + readBy, Conversation.participants (restructured to `[{user, userType}]` subdoc) + lastMessage.sender + blockedBy, Report.reporter, SupportTicket.user + messages.sender, ActivityLog.actor. Values are model names ('Guest'\|'Host'\|'Admin') | Done |
| I168 | 2026-04-17 | Subdomain migration Phase 1 — auth middleware + controller rewrite | AMS | `middleware/auth.js protect` loads user from correct model via JWT's `userType` (backward-compat reads legacy `role`); `authorize()` checks `userType`. JWT payload now includes `userType` alongside legacy `role`. `authController` adds `inferUserTypeFromOrigin(origin)` (admin.→admin, business.→host, else guest) and routes register/login/refresh/getMe/updateProfile/changePassword/linkPhone/linkEmail/deleteAccount/forgotPassword/resetPassword to the correct collection. `upgradeToHost` deprecated (returns 410 Gone). `toggleWishlist` now guest-only | Done |
| I169 | 2026-04-17 | Subdomain migration Phase 1 — controllers updated for 3-model auth | AMS | `adminController` queries all 3 collections for user listing, aggregates stats by collection, removes `make_host`/`make_admin` actions (returns 410). `otpController` registers test accounts (500000001/2/3) into the collection matching Origin or mapped role. `publicHostController` queries Host directly. `reportController` resolves target user across collections, adds `reporterType`. `messageController` uses refPath population and `{user, userType}` subdoc structure for participants. `bnplController` uses Guest. `notificationController`, `propertyController`, `wishlistController`, `seed.js` route updated accordingly | Done |
| I170 | 2026-04-17 | Subdomain migration Phase 1 — frontend userType field | AMS | `User` type gets optional `userType: 'guest'\|'host'\|'admin'` field (legacy `role` kept). `AuthContext.getRoleRedirect()` accepts either. `middleware.ts` decodes JWT and prefers `userType` over `role`. No user-visible change; subdomain routing ships in Phase 2 | Done |
| I171 | 2026-04-17 | Subdomain migration Phase 2 — middleware subdomain rewriting | AMS | `frontend/src/middleware.ts` detects host header, rewrites `business.hostn.co/*` → `/host/*` and `admin.hostn.co/*` → `/admin/*` internally. Clean URLs on subdomains (no `/host` prefix visible). Legacy `hostn.co/host/*` and `hostn.co/admin/*` return 404. Role-based protection enforced post-rewrite. Matcher expanded to cover all paths except static assets | Done |
| I172 | 2026-04-17 | Subdomain migration Phase 2 — URL helper + AuthContext | AMS | New `frontend/src/lib/urls.ts` with `URLS` object (main/business/admin base URLs from env), `detectSubdomain(hostname)`, `getPostLoginUrl()`, and `AUTH_URLS` cross-subdomain helpers. `AuthContext.getRoleRedirect()` now subdomain-aware — returns `/` on business/admin subdomains (middleware rewrites to /host or /admin), `/dashboard` on main | Done |
| I173 | 2026-04-17 | Subdomain migration Phase 2 — env vars + deployment guide | AMS | Added `NEXT_PUBLIC_MAIN_URL`, `NEXT_PUBLIC_BUSINESS_URL`, `NEXT_PUBLIC_ADMIN_URL` to `.env.production` and `.env.local.example`. Dev uses `*.localhost:3000` (Chrome/Firefox native support, no /etc/hosts needed). Created `docs/SUBDOMAIN_DEPLOYMENT.md` with step-by-step infra checklist (migration, Vercel domains, DNS CNAMEs, Railway CORS env var, smoke test) | Done |
| I174 | 2026-04-18 | Business subdomain URL polish — redirect legacy prefix + clean nav hrefs | AMS | Middleware now 308-redirects `business.hostn.co/host*` → `business.hostn.co/*` and `admin.hostn.co/admin*` → `admin.hostn.co/*`. Updated all 11 host nav items in `Sidebar.tsx`, 12 items in `HostSidebar.tsx`, logo link, and all internal `Link` / `router.push` paths across 10 host pages (listings, finance, calendar, pricing, page.tsx, etc.) to drop the `/host` prefix. URLs now stay clean throughout the entire host dashboard flow | Done |
| F203 | 2026-04-18 | Host listings — "Copy link" shared wrong domain | AMS | Property share URL was built from `window.location.origin` which returned `https://business.hostn.co`. Property detail pages live on the main domain, so shared links pointed at a blank page. Changed to `${URLS.main}/property/${id}` where `URLS.main` resolves to `https://hostn.co` in prod | Done |
| I175 | 2026-04-18 | Auth page polish on subdomains — hide Header + center form | AMS | On `business.hostn.co/auth` and `admin.hostn.co/auth`, the guest-site `<Header />` is now hidden and the login card is vertically + horizontally centered with flexbox. Main domain `hostn.co/auth` is unchanged. Uses `detectSubdomain()` from `lib/urls.ts` to decide | Done |
| I176 | 2026-04-18 | Auth page polish — unify across all domains | AMS | Login page at `/auth` now always has no Header and a vertically centered form, regardless of domain. Previously `hostn.co/auth` still showed the guest-site Header while the subdomains didn't — unified to the minimal centered experience for consistency. Side benefit: eliminates the brief Header flash on subdomains (no longer client-side conditional) | Done |
| I177 | 2026-04-18 | Auth page — Hostn logo + language toggle at top | AMS | Added a minimal top row above the welcome heading: Hostn word-mark links to `/`, language toggle with Globe icon calls `toggleLanguage()` from LanguageContext. Gives users a way to navigate home or switch language without a full Header | Done |
| F204 | 2026-04-18 | Finance layout — Remittances tab falsely highlighted on /finance | AMS | Tab bar's active check was `isActive(href) \|\| (isIndex && href === tabs[0].href)` — the second clause made Remittances appear selected when on the /finance overview root. Removed the isIndex fallback so no tab is highlighted on the overview page | Done |
| I178 | 2026-04-18 | Recent Bookings on host dashboard — Arabic title + unit column | AMS | `getRecentBookings` in hostController now populates `unit` with `nameEn nameAr`. Frontend table uses `isAr ? titleAr : title` (with fallback) and adds a new "Unit" column. Property names now display in Arabic when language is Arabic | Done |
| F205 | 2026-04-18 | Bookings table — inconsistent date format between check-in and check-out | AMS | Check-in was showing `Nov 1` while check-out showed `Nov 2, 2023`. Both now use `{ month: 'short', day: 'numeric', year: 'numeric' }` for consistency | Done |
| I179 | 2026-04-18 | Mini-calendar — vertical spacing between day rows | AMS | Day cells bumped from `w-7 h-7` (28×28px) to `w-8 h-8` (32×32px) and grid got `gap-y-1.5` (6px vertical gap). Previously rows were visually squished with status highlights overlapping | Done |
| I180 | 2026-04-18 | Booking reference number (HB-XXXXXXXX) | AMS | `Booking` schema adds `reference` field, auto-generated in pre-save hook as `HB-` + 8 random alphanumeric chars (readability-optimized: no 0/O/1/I). Unique + indexed. Random not sequential to avoid revealing booking volume. Collision retry up to 5 times. `backfill-booking-reference.js` script generates values for existing bookings. Displayed in bookings table and booking detail panels | Done |
| F206 | 2026-04-18 | Booking status update — "Failed to update booking" on every click | AMS | After Phase 1 the Notification schema required `userType`, but 21 `Notification.createNotification()` calls across 8 controllers were missing it. This caused `validation error` on every booking status update (confirm/cancel/reject), surfacing as "Failed to update booking" in the UI (though the booking itself did save). Audited + fixed all 21 callers in bookingController, paymentController, reviewController, supportController, etc. | Done |
| F207 | 2026-04-18 | Messages page — guest/host name shown as "Unknown" | AMS | Frontend's `getOtherParticipant` was reading `participants.find(p => p._id)` but Phase 1 changed the shape to `[{user: User, userType}]` subdocs. The fallback `name \|\| 'Unknown'` kicked in every time. Updated both `host/messages/page.tsx` and `dashboard/messages/page.tsx` to handle the new subdoc shape (with backward-compat for legacy flat refs) | Done |
| I181 | 2026-04-18 | Bookings page — expandable rows, sort, reference column, view-invoice button | AMS | Added `ChevronDown`/`ChevronUp` expand controls that reveal 3-column panel (Booking Details / Payment Details / Booking Summary) matching Gathern-style detail view. Sort dropdown with "Newest / Oldest / By check-in". New "Reference" column shows `HB-XXXXXXXX`. Confirmed bookings with an associated invoice get a `FileText` icon button linking to `/finance/invoices/<id>`. Backend `getHostBookings` now also looks up invoices per booking and attaches them | Done |
| I182 | 2026-04-18 | Invoice detail page | AMS | New page at `/finance/invoices/[id]` — printable invoice sheet with header (Hostn branding, invoice number, issued date + status badge), period meta, breakdown (gross bookings, commission rate/amount, VAT 15%, total due), and a table of bookings included (with reference, guest, property, dates, amount). "Print" button triggers `window.print()`. Backend `getInvoiceDetail` updated to also populate `reference` on nested bookings | Done |
